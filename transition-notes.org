* Finmag Transition Notes

** Purpose

This file records concrete environment and packaging findings relevant to the
transition from:

- Python 2 to Python 3
- old `dolfin` packaging to a reproducible Python 3 runtime for the same
  DOLFIN generation

The emphasis here is on exact versions, package sources, and constraints that
were observed directly during probing.

** Strategic Direction

The transition is now treated as four separate milestones, not one continuous
upgrade:

1. Python 3 usability on the current legacy =dolfin 2017.1.0= transition
   stack;
2. a user-facing installation path outside the specialist container workflow,
   preferably via =pixi=;
3. migration to legacy FEniCS/=dolfin 2019.1.0=;
4. a later, reduced-scope =dolfinx= port.

This matters because legacy FEniCS and =dolfinx= are different targets with
different risks. The last legacy FEniCS release is =2019.1.0=; =dolfinx= is a
separate actively developed stack and should be handled as a redesign/porting
track rather than a routine package upgrade.

Guiding rule:

- do not spend time reviving old features unless that work directly helps one
  of the four milestones above.

In practice this means:

- keeping historical xfails and niche comparison paths deferred when they do
  not block Python 3, =pixi=, =dolfin 2019.1.0=, or =dolfinx=;
- preferring a small stable workflow matrix over broad historical parity;
- using the current Python 3 core gate as the main M1 regression baseline.

** M2 Dependency Inventory For `pixi`

This section records the first packaging-oriented dependency inventory for the
`pixi` track.

*** Important structural finding

The repository does not currently contain a modern top-level Python packaging
file such as:

- `pyproject.toml`
- `setup.py`
- `setup.cfg`
- `pixi.toml`

The only `setup.py` found in the tree is:

- `dev/setup.py`

and that file is only a historical helper for installing an already-prepared
tree from `/tmp/finmag/`. So M2 is not only a dependency-translation task; it
also needs an explicit packaging decision for Finmag itself.

*** Current Python 3 transition image inputs

The working Python 3 transition image currently installs, from the Debian
snapshot and a small amount of pip/GitHub glue:

- Python runtime and build tools:
  - `python3`
  - `python3-dev`
  - `python3-pip`
  - `python3-setuptools`
  - `cython3`
  - `g++`
  - `make`
- scientific Python stack:
  - `numpy`
  - `scipy`
  - `matplotlib`
  - `pytest`
- legacy FEniCS stack:
  - `python3-dolfin = 2017.1.0-4`
  - `python3-ffc = 2017.1.0-2`
  - `python3-ufl = 2017.1.0-2`
  - `python3-dijitso = 2017.1.0-2`
  - `python3-fiat = 2017.1.0-2`
  - `python3-instant = 2017.1.0-2`
- PETSc / SLEPc layer:
  - `python3-petsc4py = 3.7.0-3+b2`
  - `python3-slepc4py = 3.7.0-3+b1`
  - matching PETSc / SLEPc / DOLFIN development libraries
- native build / runtime libraries:
  - `libsundials-dev = 2.7.0+dfsg-2`
  - `libboost-python-dev`
  - `libboost-test-dev`
  - `libhdf5-openmpi-dev`
  - `libhdf5-mpi-dev`
  - `libnetcdf-dev`
  - `libvtk6-dev`
  - `libgdal-dev`
  - `default-libmysqlclient-dev`
  - image-format and compression headers such as `libpng-dev`, `libtiff-dev`,
    `zlib1g-dev`
- mesh and geometry tools:
  - `gmsh`
  - `netgen`
- visualization / headless runtime support:
  - `xvfb`
  - `xauth`
  - `tix`
  - `tcl-dev`
  - `tk-dev`
- extra external components:
  - `dolfinh5tools` from GitHub via `pip`
  - `oommf` from `https://github.com/fangohr/oommf`

*** Likely `pixi` / conda-manageable today

Based on the current codebase imports, the transition image, and current
conda-forge package availability, the following parts look feasible for a
`pixi` environment:

- core Python toolchain:
  - `python`
  - `pip`
  - `setuptools`
  - `cython`
  - `pytest`
- scientific Python stack:
  - `numpy`
  - `scipy`
  - `matplotlib`
- legacy FEniCS track:
  - `fenics = 2019.1.0` is available on conda-forge
- FEniCSx track:
  - `fenics-dolfinx` is available on conda-forge
- PETSc / SLEPc layer:
  - `petsc`
  - `petsc4py`
  - `slepc`
  - `slepc4py`
- meshing / geometry:
  - `gmsh`
  - `python-gmsh`
  - `netgen`
- visualization:
  - `paraview`
- external solver:
  - `oommf`

This is encouraging for M2 and also for later M3/M4 work, because it means the
broader numerical and meshing ecosystem is not obviously blocked on package
availability alone.

*** Likely external or project-specific work, even with `pixi`

The following items look like they will still need custom handling:

- Finmag packaging itself
  - there is no modern top-level package metadata yet
  - M2 needs a real install layout, not just dependency installation
- `dolfinh5tools`
  - this is an external companion package, not part of Finmag
  - the current Python 3 image installs it directly from GitHub and patches it
  - for `pixi`, this likely needs either:
    - a pip dependency from GitHub, or
    - a separate package recipe / packaging story
- Finmag native extensions
  - `native/Makefile` still drives the build
  - M2 needs a decision on whether `pixi` invokes that build explicitly or
    whether packaging is taught to build/install these extensions
- OOMMF integration details
  - package availability now looks good, but Finmag depends on the
    Southampton/Fangohr fork and on executable discovery conventions
  - M2 should verify that the conda-forge `oommf` package is functionally
    equivalent for Finmag's comparison helpers
- Netgen runtime behavior
  - package availability is not the same as runtime reliability
  - the current Python 3 transition image still needs compatibility handling
    for the historical `netgen 4.9.13` shutdown bug and Tcl/Tix paths
  - a `pixi` environment will need its own probe and validation path

*** Likely blockers for a full M2 environment on the current 2017 stack

These look like the main risks for a full `pixi` environment that reproduces
the current M1 stack exactly:

- exact legacy `dolfin 2017.1.0`
  - the current working environment depends on a Debian snapshot package set
  - the conda-forge route is much more naturally aligned with legacy
    `fenics 2019.1.0` than with this exact `2017.1.0` stack
- direct parity with the current container's native-library mix
  - the container uses historical Debian package versions and local native
    compatibility fixes
  - `pixi` may be an easier fit once the project moves to M3
- historical optional tool paths
  - `nsim`
  - Mercurial / `hg`
  - GUI-heavy Paraview workflows
  - these should not block M2 unless we decide they are part of the supported
    user-facing environment

*** Current recommendation for M2

Do not aim first for:

- "reproduce the whole M1 Debian-snapshot stack in pixi"

Aim first for:

- a `pixi`-managed environment for the Python-facing Finmag package and its
  practical workflows,
- most likely using conda-forge `fenics 2019.1.0` as the more natural legacy
  FEniCS packaging target,
- while treating exact `2017.1.0` parity as a container-only reference.

That suggests the packaging track and the M3 legacy-FEniCS-2019 track may be
partly coupled in practice.

*** Proposed first `pixi` target

The first `pixi` attempt should be intentionally narrow.

Recommended target environment contents:

- `python`
- `pip`
- `setuptools`
- `cython`
- `numpy`
- `scipy`
- `matplotlib`
- `pytest`
- `fenics = 2019.1.0`
- `petsc4py`
- `slepc4py`
- `gmsh`
- `netgen`

Recommended initial omissions:

- `oommf`
- `paraview`
- GUI/rendering support
- `nsim`
- Mercurial / `hg`
- any attempt to preserve exact `dolfin 2017.1.0` package parity

Recommended first acceptance checks for M2:

1. `pixi run import-finmag`
2. `pixi run barmini-construct`
3. `pixi run barmini-smoke`

Recommended interpretation of the outcomes:

- if step 1 fails, the blocker is packaging/import-level and M2 should stay
  focused there;
- if steps 1 and 2 pass but step 3 fails, M2 has still made real progress,
  but the native-build/runtime path needs a second pass;
- if all three pass, M2 has a credible first user-facing environment even
  before optional tools are layered in.

*** Suggested next M2 deliverables

In order:

1. add a first `pixi.toml` to the repo, even if incomplete;
2. add a minimal packaging/install story for Finmag itself;
3. document the exact `pixi` commands for environment creation and the first
   acceptance checks above;
4. only then broaden to optional tools such as OOMMF or visualization.

*** First `pixi.toml` draft

The repository now contains:

- `pixi.toml`

This first draft is intentionally a packaging probe, not a claim of immediate
success. It currently targets:

- conda-forge
- `linux-64`
- `python 3.11`
- `fenics 2019.1.0`
- `petsc4py`
- `slepc4py`
- `gmsh`
- `netgen`
- the standard scientific Python stack needed by the current Finmag code

The initial tasks in that file mirror the proposed first M2 acceptance checks:

- `pixi run import-finmag`
- `pixi run barmini-construct`
- `pixi run barmini-smoke`

The tasks currently rely on:

- `PYTHONPATH=src`

rather than a real package installation, because Finmag still lacks modern
top-level packaging metadata. That is acceptable for the first probe, but it
is not the final M2 install story.

*** Verified first-pass `pixi` status

The current verified position is:

- `pixi run import-finmag` works against conda-forge `fenics 2019.1.0`
- `pixi run barmini-construct` works with the full default
  `finmag.example.barmini()` setup
- `pixi run barmini-smoke` works through `run_until(1e-12)` on the default
  Sundials backend
- `pixi run restart-smoke` works through save/restart/reload on the default
  Sundials backend
- the verified smoke path uses the full default `barmini`, including FK
  demag, FEniCS 2019.1.0, and the native Sundials 7.2.1 wrapper

The `pixi.toml` tasks `barmini-construct` and `barmini-smoke` have been
aligned to this stronger verified workflow, and `restart-smoke` extends that
matrix to state persistence.

*** Aggregate M2 witness and CI

The pixi side now has a single aggregate witness, analogous to the Docker-side
=dev/bin/verify-python3-m1= script:

- `dev/bin/verify-python3-m2`
  - runs:
    - `pixi run import-finmag`
    - `pixi run barmini-construct`
    - `pixi run barmini-smoke`
    - `pixi run restart-smoke`
  - exports `XDG_CACHE_HOME=/tmp/pixi-cache` and
    `PIXI_CACHE_DIR=/tmp/pixi-cache` by default so the pixi witness does not
    recreate repo-local state that would later interfere with the
    Docker-copying M1 helpers. [Codex GPT-5.4]

There is now a matching GitHub Actions workflow:

- `.github/workflows/python3-m2.yml`
  - uses `prefix-dev/setup-pixi@v0.9.3`
  - pins `pixi-version: v0.62.1`
  - enables action-managed cache reuse
  - runs `dev/bin/verify-python3-m2`

This makes the pixi / FEniCS-2019 path visible in CI as a workflow-level M2
usability witness, rather than only as manually remembered `pixi run ...`
commands. [Codex GPT-5.4]

*** First explicit M3 / FEniCS-2019 pytest gate

The first curated pytest subset under conda-forge `fenics 2019.1.0` is now
verified and promoted to an explicit M3 gate.

Verified result:

- `7 passed, 15 warnings in 14.44s`

Files in the first gate:

- `src/finmag/tests/test_effective_field.py`
- `src/finmag/tests/test_writing_data.py`
- `src/finmag/drivers/tests/test_integrator_raises_exception_on_exceed_maxsteps.py`
- `src/finmag/tests/test_restart_simulation.py`

The gate has since been widened with passing FEniCS-2019 checks for:

- `src/finmag/tests/test_sim_ode.py`
- `src/finmag/tests/test_applied_field.py`
- `src/finmag/tests/test_exchange_static.py`
- `src/finmag/tests/test_llg.py`
- `src/finmag/util/ode/tests/test_sundials_ode.py`
- `src/finmag/tests/test_energy_creation_with_variable_Ms.py`
- `src/finmag/tests/energy_density/test_energy_density.py`
- `src/finmag/tests/test_anis.py`
- `src/finmag/tests/test_dmi.py`
- `src/finmag/tests/test_dmi_terms.py`
- `src/finmag/tests/test_heun.py`
- `src/finmag/tests/test_spatially_varying_alpha.py`
- `src/finmag/tests/test_spatially_varying_anisotropy.py`
- `src/finmag/tests/test_unit_length.py`
- `src/finmag/tests/test_interactions_scale_linearly_with_m.py`
- `src/finmag/energies/exchange_test.py`
- `src/finmag/energies/anisotropy_test.py`
- `src/finmag/energies/cubic_anisotropy_test.py`
- `src/finmag/energies/demag/fk_demag_test.py`
- `src/finmag/energies/demag/fk_demag_2d_test.py`
- `src/finmag/energies/test_energies_in_regions.py`
- `src/finmag/energies/thin_film_demag_test.py`
- `src/finmag/tests/test_1d_domain_wall_profile_uniaxial_anisotropy.py`

- `src/finmag/tests/energy_density/test_energy_density.py`
  keeps two explicit dependency skips in the M3 gate:
  - `test_exchange_energy_density` when `nsim` is unavailable
  - `test_demag_energy_density` when `netgen` is unavailable
  Those are now explicit environment-conditioned skips rather than hard
  failures on the pixi path. [Codex GPT-5.4]

Focused SUNDIALS-7 fix:

- `native/src/sundials/sundials_cvode_impl.h`
  - the SUNDIALS-7 wrapper now attaches an explicit nonlinear solver in
    `cvode::init(...)`
  - `CV_FUNCTIONAL` uses `SUNNonlinSol_FixedPoint(...)`
  - `CV_NEWTON` uses `SUNNonlinSol_Newton(...)`
  - this fixes the previous pixi/FEniCS-2019 regression where
    `test_simple_1d` in `test_sundials_ode.py` exited nonzero before pytest
    could report a normal failure. [Codex GPT-5.4]

Additional DOLFIN-2019 / pytest compatibility fixes for the expanded M3 gate:

- `src/finmag/tests/test_spatially_varying_alpha.py`
- `src/finmag/energies/exchange_test.py`
- `src/finmag/energies/anisotropy_test.py`
- `src/finmag/energies/cubic_anisotropy.py`
  - switched remaining legacy assembled-vector / PETSc-vector `.array()` calls
    to `.get_local()` on the FEniCS-2019 path. [Codex GPT-5.4]
- `src/finmag/tests/test_interactions_scale_linearly_with_m.py`
  - replaced the old `pytest.mark.xfail((...))` parametrization form with
    `pytest.param(..., marks=pytest.mark.xfail)` so the file collects cleanly
    under modern pytest. [Codex GPT-5.4]
- `src/finmag/energies/demag/fk_demag.py`
  - the LU demag path now treats `reuse_factorization` as an optional hint,
  because DOLFIN 2019 no longer exposes that parameter on all LU backends.
  [Codex GPT-5.4]
- `src/finmag/energies/demag/fk_demag_2d.py`
  - updated `MeshEditor` usage to the DOLFIN-2019 signatures
  - switched assembled nodal-energy vectors to `.get_local()`
- `src/finmag/energies/thin_film_demag.py`
- `src/finmag/tests/test_1d_domain_wall_profile_uniaxial_anisotropy.py`
  - switched remaining DOLFIN 2019 vector accessors from `.array()` to
    `.get_local()`
- `src/finmag/util/helpers.py`
- `src/finmag/field.py`
  - Python-backed expressions now go through `UserExpression`, and
    `Field.from_expression()` recognizes both `Expression` and
    `UserExpression` on the FEniCS-2019 path. [Codex GPT-5.4]
- `src/finmag/energies/test_energies_in_regions.py`
  - `CellFunction` replaced with `MeshFunction` for DOLFIN 2019

Demag-heavy M3 additions currently come with explicit environment-conditioned
skips rather than hard failures:

- `fk_demag_test.py`
  - skips when `netgen` is not usable
- `test_energies_in_regions.py::test_energies_in_separated_subdomains`
  - skips when `netgen` is not usable
- `demag_pbc_test.py`
  - is now in the M3 gate as a collected skip when
    `finmag.native.treecode_bem` is unavailable. It uses a module-level
    `skipif` marker rather than `pytest.importorskip(...)`, so focused pixi
    runs report explicit skips instead of pytest's no-tests-collected exit.
    [Codex gpt-5.5 high]

This subset is exposed in three places:

- `pixi run barmini-suite`
- `dev/bin/verify-python3-m3`
- `.github/workflows/python3-m3.yml`

This is the first point where M3 becomes more than a packaging/runtime smoke
story: the `fenics 2019.1.0` path now has a small but real pytest regression
gate built around the historical `barmini` acceptance slice. [Codex GPT-5.4]

*** Native Sundials port status

The previous main runtime gap for M2 was the native Sundials backend. That gap
is now closed for the first practical workflow.

Verified result:

- `from finmag.native import sundials` works in the `pixi` environment
- `sundials.get_sundials_version()` reports `7.2.1`
- `finmag.example.barmini().run_until(1e-12)` completes on the default
  Sundials backend in `pixi`

Key porting points:

- `native/src/sundials/sundials_cvode_impl.h`
  - conditional SUNDIALS 7 include path
  - `SUNContext` lifecycle handling
  - modern `CVodeSetLinearSolver` / `CVodeSetJacTimes` /
    `CVodeSetPreconditioner` wiring
  - compatibility aliases for pre-SUNDIALS-6 type and constant names
  - cloned `N_Vector` template retained from `init()` so iterative solvers do
    not depend on `CVodeGetCurrentState()` being populated during setup
  - synthetic scratch vectors supplied to Python preconditioner callbacks on
    the SUNDIALS 7 path to preserve the historical Python callback signatures
- `native/Makefile`
  - `sundials.so` is back in the `pixi` native module set
  - the module now links explicitly against the split SUNDIALS 7 CVODE,
    SUNLINSOL, SUNMATRIX, and core libraries

The FK demag path itself is no longer the main blocker:

- a new array-based native BEM entry point now bypasses the old
  `BoundaryMesh -> std::shared_ptr` Boost.Python conversion assumption
- this is enough for the default `barmini` construction and short SciPy-backed
  run under `fenics 2019.1.0`

** Current Reference Environment

Reference container:

- `finmag/finmag:latest`

Verified contents:

- `python` = Python 2.7.12
- `python3` = Python 3.5.x
- `python` can import `dolfin`
- `python3` cannot import `dolfin`

Verified DOLFIN version in the reference container:

- `dolfin.__version__ == 2017.1.0`

Installed package metadata observed in the container:

- `python-dolfin` = `2017.1.0-1~ppa3~xenial1`
- `libdolfin2017.1` = `2017.1.0-1~ppa3~xenial1`
- `libdolfin-dev` = `2017.1.0-1~ppa3~xenial1`
- `python-ffc` = `2017.1.0-1~ppa1~xenial1`
- `python-fiat` = `2017.1.0-1~ppa1~xenial1`
- `python-ufl` = `2017.1.0-1~ppa1~xenial1`

Observed package source configuration in the container:

- binary packages come from `ppa:fenics-packages/fenics`
- the `deb-src` entry for that PPA was commented out by default

** Important Constraint

Ubuntu/xenial packaging in the current Finmag image provides:

- `python-dolfin`

but not:

- `python3-dolfin`

for the installed `2017.1.0` stack.

This is why the Python 3 port can proceed with syntax and import cleanup, but
cannot yet run Finmag end to end under Python 3 in the current image.

** Upstream / Packaging Findings

*** DOLFIN 2017.1.0

During probing we established that upstream `dolfin 2017.1.0` switched the
default build to Python 3, with Python 2 requiring:

- `-DDOLFIN_USE_PYTHON3=off`

This is important because it means a Python 3 build of the 2017 generation is
not conceptually alien to DOLFIN itself; the limitation is mainly packaging and
dependency availability in the current reference image.

*** PPA Source Mismatch

After enabling the PPA `deb-src` entry in a disposable container, `apt-get
source dolfin` did not fetch `2017.1.0`. It fetched:

- `dolfin 2019.1.0-1~ppa0~xenial2`

This means the current PPA source state no longer matches the installed binary
`2017.1.0` packages in `finmag/finmag:latest`.

So:

- the PPA is useful to inspect newer packaging patterns,
- but not sufficient to reconstruct the exact old source package used by the
  reference image.

*** Debian Snapshot With Matching Python 3 Packages

The decisive finding came from Debian snapshot probing.

Snapshot tested:

- `http://snapshot.debian.org/archive/debian/20170920T000000Z sid`

Observed package availability there:

- `python3-dolfin = 2017.1.0-4`
- `python3-ffc = 2017.1.0-2`
- `python3-ufl = 2017.1.0-2`
- `python3-dijitso = 2017.1.0-2`
- `python3-fiat = 2017.1.0-2`
- `python3-instant = 2017.1.0-2`
- `python3-petsc4py = 3.7.0-3+b2`
- `python3-slepc4py = 3.7.0-3+b1`

This is the first verified source of a historically matching Python 3 DOLFIN
stack for the 2017 generation.

The same snapshot also contains the corresponding source package:

- `dolfin = 2017.1.0-4`

The package metadata and build logs for this generation show Debian/Ubuntu
building with both Python 2 and Python 3 via:

- `dh ... --with python2,python3`

and using:

- `-D DOLFIN_USE_PYTHON3:BOOL=OFF` for the Python 2 build directories

which confirms that the `2017.1.0-4` packaging is a real dual-runtime baseline.

** Snapshot Package Resolution Notes

Initial install probing against the `20170920T000000Z sid` snapshot showed:

- installing `python3-dolfin=2017.1.0-4` alone is insufficient
- it requires `libdolfin-dev=2017.1.0-4`
- `libdolfin-dev=2017.1.0-4` in turn requires additional dev/runtime packages,
  including:
  - `libhdf5-mpi-dev`
  - `libscotch-dev`
  - `libvtk6-dev`
  - `libpetsc3.7.6-dev`
  - `libslepc3.7.4-dev`

This means the snapshot image needs either:

1. a broader install of the full matching dependency closure from the same
   snapshot, or
2. a staged `dist-upgrade` onto that historical snapshot before package
   installation.

The experimental Dockerfile under:

- `install/docker/python3-dolfin2017-snapshot/Dockerfile`

follows the second approach.

*** Exact package versions currently targeted

The current experimental Python 3 image is pinned to the following package
versions from the same snapshot:

- `libdolfin-dev = 2017.1.0-4`
- `python3-dolfin = 2017.1.0-4`
- `python3-ffc = 2017.1.0-2`
- `python3-ufl = 2017.1.0-2`
- `python3-dijitso = 2017.1.0-2`
- `python3-fiat = 2017.1.0-2`
- `python3-instant = 2017.1.0-2`
- `python3-petsc4py = 3.7.0-3+b2`
- `python3-slepc4py = 3.7.0-3+b1`
- `libpetsc3.7.6-dev`
- `libslepc3.7.4-dev`
- `zlib1g = 1:1.2.8.dfsg-5`

In addition to the DOLFIN-specific packages, the current install attempt also
pulls in development headers required by the historical dependency closure,
including:

- `libhdf5-openmpi-dev`
- `libhdf5-mpi-dev`
- `libptscotch-dev`
- `libscotch-dev`
- `libvtk6-dev`

The `zlib1g` pin was added because the resolver otherwise tried to combine:

- `zlib1g-dev = 1:1.2.8.dfsg-5`

with:

- `zlib1g = 1:1.2.8.dfsg-5+deb9u1`

which is not accepted by `apt`.

** Comparison With Other Archive Routes

Other archive probes were less suitable:

- Ubuntu 18.04 (`bionic`) offers `dolfin 2017.2.0.post0-2`, not `2017.1.0`
- Debian `stretch` snapshot route provided `dolfin 2016.2.0-2`, not `2017.1.0`

Therefore the currently best historically matching route for a Python 3 DOLFIN
runtime is:

- Debian snapshot `20170920T000000Z sid`

** Working Assumption Going Forward

For the purpose of getting Finmag running under Python 3 with an old DOLFIN
stack, the most promising path is:

1. build or install a Python 3 environment from the Debian snapshot carrying
   `python3-dolfin=2017.1.0-4`,
2. copy the Finmag checkout into that environment,
3. continue validating:
   - `python3 -c "import dolfin"`
   - `python3 -c "import finmag"`
   - then progressively larger test subsets.

** Current Build Result

The experimental image defined in:

- `install/docker/python3-dolfin2017-snapshot/Dockerfile`

now builds successfully as:

- `finmag-py3-dolfin2017`

Verified build-time import result:

- `python3 = 3.5.4`
- `dolfin.__version__ = 2017.1.0`
- `dolfin.__file__ = /usr/lib/python3/dist-packages/dolfin/__init__.py`

Verified runtime import result in the built image:

- `docker run --rm -e OMPI_MCA_plm=isolated finmag-py3-dolfin2017 ...`

and again:

- `python3 = 3.5.4`
- `dolfin.__version__ = 2017.1.0`

** Open MPI Runtime Note

Importing `dolfin` in this snapshot-based image triggers MPI initialization.
In both Docker build and container runtime contexts, this required:

- `OMPI_MCA_plm=isolated`

Without that override, `import dolfin` failed with Open MPI errors about:

- `plm_rsh_agent`
- `Unable to start a daemon on the local node`
- `MPI_Init_thread`

So the current working assumption is that any Python 3 Finmag test runs in this
image should start with at least:

- `OMPI_MCA_plm=isolated`

** OOMMF Runtime Notes

The Python 3 transition image now installs the Southampton/Fangohr OOMMF fork
from:

- =https://github.com/fangohr/oommf=

This fork is needed rather than a plain system OOMMF because Finmag's OOMMF
comparison helpers reference local extensions such as:

- =Southampton_CubicAnisotropy8=
- =Southampton_UniaxialAnisotropy4=
- DMI and PBC extension objects compiled under =app/oxs/local=

Build/runtime constraint:

- OOMMF refuses to run as root.

The Dockerfile therefore:

- creates an =oommfbuild= user,
- clones and builds OOMMF as that user with =make build=,
- installs a small =/usr/local/bin/oommf= wrapper around
  =/usr/local/oommf/oommf/oommf.tcl=,
- verifies =oommf +version= as =oommfbuild=,
- switches the final image runtime user to =oommfbuild=.

Verified OOMMF version in the image:

- =OOMMF 2.1a0=

Python 3 fixes required after OOMMF became available:

- =src/finmag/util/oommf/ovf.py= now opens OVF files in binary mode and
  decodes only line-oriented ASCII headers, because OOMMF writes binary OVF
  payloads that cannot be read through Python 3 text decoding.
- =src/finmag/util/oommf/lattice.py= now uses identity checks for optional
  NumPy-array data, avoiding ambiguous array truth-value errors.

Targeted OOMMF regression subset verified in the transition image:

- =src/finmag/tests/comparison/test_dmdt.py=
- =src/finmag/tests/comparison/anisotropy/test_anis_oommf.py=
- =src/finmag/tests/comparison/anisotropy/test_cubic_anis_oommf.py=
- =src/finmag/tests/comparison/exchange/test_exchange_field.py=
- =src/finmag/tests/oommf/test_anisotropy.py=
- =src/finmag/tests/oommf/test_exchange.py=

Result:

- targeted OOMMF subset passed under Python 3

The active GitHub Actions core-suite workflow and local
=dev/bin/verify-python3-core-suite= already list these files. With OOMMF now
installed in the image, they run as real regression tests instead of
external-tool skips.

** Native Build Progress

The Python 3 container path now goes beyond plain import:

- =import finmag= works
- constructing =finmag.example.barmini(...)= works
- =sim.run_until(1e-12)= works
- the first =barmini=-based acceptance subset now passes under Python 3

Current verified subset:

- =src/finmag/tests/test_effective_field.py=
- =src/finmag/tests/test_writing_data.py=
- =src/finmag/drivers/tests/test_integrator_raises_exception_on_exceed_maxsteps.py=
- =src/finmag/tests/test_restart_simulation.py=

Verified result:

- =7 passed in 58.73s=

The remaining issue that had blocked this subset was Python-3-specific `.ndt`
readback in =Tablereader=. The old =numpy.genfromtxt= path was replaced with
explicit line parsing, which avoids the Python 3.5 / NumPy 1.13 bytes-text
mismatch seen in the snapshot image.

For manual verification:

- =dev/bin/verify-python3-barmini-smoke=
- =dev/bin/verify-python3-barmini-suite=

The GitHub Actions transition workflow now runs the =barmini=-first subset
instead of only the one-step smoke test.

** Minimal Acceptance Suite Progress

The agreed minimal acceptance suite now also runs successfully in the Python 3
transition container.

Verified command:

- =dev/bin/verify-python3-minimal-suite=

Verified result:

- =21 passed in 125.69s=

Included tests:

- =src/finmag/tests/test_effective_field.py=
- =src/finmag/tests/test_writing_data.py=
- =src/finmag/drivers/tests/test_integrator_raises_exception_on_exceed_maxsteps.py=
- =src/finmag/tests/test_restart_simulation.py=
- =src/finmag/tests/test_sim_ode.py=
- =src/finmag/tests/test_applied_field.py=
- =src/finmag/tests/test_exchange_static.py=
- =src/finmag/tests/test_llg.py=
- =src/finmag/util/ode/tests/test_sundials_ode.py=

The transition image now includes:

- =python3-scipy=
- =python3-matplotlib=

Those were added from the same Debian snapshot used for the DOLFIN 2017 stack.
The install line now uses =--no-install-recommends= to avoid dragging in a very
large TeX/doc recommendation set.

As a result, =OdeSundialsTests.test_simple_1d_scipy= now runs normally instead
of skipping.

For =test_sim_ode.py=, the lazy =matplotlib= import was kept intentionally.
That test only needs plotting when =do_plot=True=, so importing =matplotlib=
inside the plotting branch keeps normal test collection lighter and avoids
making plotting a hard import-time dependency of the test module.

** Core Transition Suite Progress

The transition CI now goes beyond the minimal acceptance suite and runs a
broader Python 3 core suite.

Verified result:

- =356 passed, 16 skipped, 6 xfailed, 13 warnings in 1796.25s=

This full rerun now includes the previously targeted fixes:

- =src/finmag/sim/sim_helpers_test.py::test_get_submesh= passes
- =src/finmag/sim/sim_test.py::TestSimulation::test_mark_regions= passes
- =src/finmag/sim/sim_test.py::TestSimulation::test_pbc2d_m_init= passes
- =src/finmag/tests/test_dmi_terms.py= now contains three real passing tests
- =src/finmag/energies/dmi_test.py::test_dmi_pbc2d= passes
- =src/finmag/energies/test_energies_in_regions.py::test_energies_in_touching_subdomains=
  passes

The existing core-suite workflow already contained those files, so CI now
reflects all of that progress without any workflow-structure change.

Additional targeted progress since that last full rerun:

- =src/finmag/energies/demag/fk_demag_2d.py=
  - fixed Python 3 NumPy index handling in =build_mapping()=
  - fixed lifting of the 2d magnetisation into the auxiliary 3d mesh
  - fixed the helper =Expression= construction in =create_dg3_from_dg2()=
- =src/finmag/energies/demag/fk_demag_2d_test.py=
  - the old content-free =xfail= was replaced by a real passing thin-film
    demag regression test
  - targeted verification: =2 passed=
- =src/finmag/sim/sim.py=
  - submesh interpolation in =get_field_as_dolfin_function(..., region=...)=
    now enables extrapolation on the source field so DOLFIN does not fail on
    numerically boundary-adjacent points
- =src/finmag/energies/zeeman_test.py=
  - =test_compare_stray_field_of_sphere_with_dipolar_field= is no longer
    marked =xfail=
  - it now uses an explicit =netgen_is_usable()= skip if the mesh-generation
    environment is not available
  - targeted verification: =1 passed=
- =src/finmag/tests/test_jacobian.py=
  - fixed Python 2 print syntax so the file collects under Python 3
  - the file now passes under Python 3
  - it has been added to =dev/bin/verify-python3-core-suite= and
    =.github/workflows/python3-core-suite.yml=
  - targeted verification: =3 passed=
- =src/finmag/sim/normal_mode_sim.py=
  - fixed mesh-region PSD extraction for this DOLFIN version by preferring
    =submesh.data().array('parent_vertex_indices', 0)= and falling back to the
    old =mesh_function(...)= API only when needed
- =src/finmag/util/fft.py=
  - fixed NumPy truth-value bugs by replacing comparisons such as
    =vertex_indices != None= with =is not None=
- =src/finmag/sim/sim.py=
  - =Simulation.profile()= now uses =cProfile.Profile().runctx(...)= with an
    explicit =sim= binding so profiling works under pytest as well as in
    normal interactive use
- =src/finmag/sim/sim_test.py=
  - =test_compute_and_plot_power_spectral_density_in_mesh_region= is no longer
    a stale =xfail=
  - =test_profile= is no longer a stale =xfail=
  - =test_setting_different_material_parameters_in_different_regions= is no
    longer a stale =xfail=
  - that test now checks region-dependent initialisation directly at
    representative points in the nanodisk and spherical-particle regions,
    instead of depending on optional Paraview tooling and ending in an
    explicit =NotImplementedError=
  - =test_regression_schedule_switch_off_field= is no longer a stale
    =skipif("True")= reminder
- =src/finmag/sim/sim.py=
  - fixed =remove_interaction()= to delete the correct =H_<interaction>=
    tablewriter callback instead of deleting =E_<interaction>= twice
  - this restores the runtime path where a scheduled field switch-off removes
    the Zeeman interaction and later =save_ndt= calls continue to work
- =src/finmag/energies/test_energies_in_regions.py=
  - removed the self-skip in the subdomain-energy consistency check after
    verifying that the sum of subdomain energies matches the total energy
    within the existing tolerance
- =src/finmag/util/helpers.py=
  - fixed =apply_vertexwise()= for the current DOLFIN API by deriving the
    geometric dimension from =u.function_space().mesh().geometry().dim()=
    instead of the removed =u.domain()=
- =src/finmag/util/helpers_test.py=
  - =test_apply_vertexwise= is no longer skipped
- =src/finmag/sim/sim_test.py=
  - replaced stale unconditional animation/export skips with checks for the
    actual external dependencies
  - =test_compute_eigenmode_animations= now runs the non-movie VTK eigenmode
    export path under Python 3 and only omits movie-export subcases when
    Paraview or =mencoder= are unavailable
- =src/finmag/util/helpers_test.py=
  - =test_jpg2avi= and =test_pvd2avi= now skip based on missing Paraview,
    =sh=, or =mencoder= instead of an unconditional marker
  - =test_get_hg_revision_info= now skips based on the missing =hg=
    executable instead of an unconditional marker, and is documented as
    historical pre-Git coverage

Long-term cleanup todo:

- remove =get_hg_revision_info()= and its Mercurial-specific test once the
  Python 3 transition is stable and historical repository tooling can be
  retired

Additional targeted verification for this follow-up batch:

- =2 passed in 62.50s=
  - =src/finmag/sim/sim_test.py::test_compute_and_plot_power_spectral_density_in_mesh_region=
  - =src/finmag/sim/sim_test.py::test_profile=

Additional targeted verification for the final substantive =sim_test.py= xfail:

- =1 passed in 57.38s=
  - =src/finmag/sim/sim_test.py::test_setting_different_material_parameters_in_different_regions=

Additional targeted verification for the old switch-off regression reminder:

- =1 passed in 56.12s=
  - =src/finmag/sim/sim_test.py::test_regression_schedule_switch_off_field=

Additional targeted verification for follow-up non-GUI cleanups:

- =1 passed in 64.48s=
  - =src/finmag/energies/test_energies_in_regions.py::test_energies_in_separated_subdomains=
- =1 passed in 79.23s=
  - =src/finmag/util/helpers_test.py::test_apply_vertexwise=

Combined targeted verification for this batch:

- =6 passed in 91.45s=
  - =src/finmag/energies/demag/fk_demag_2d_test.py=
  - =src/finmag/energies/zeeman_test.py::test_compare_stray_field_of_sphere_with_dipolar_field=
  - =src/finmag/tests/test_jacobian.py=

This broader suite adds the following green areas on top of the minimal suite:

- =src/finmag/drivers/tests/test_relaxation.py=
- =src/finmag/drivers/tests/test_relax_two_times.py=
- =src/finmag/tests/test_cyclic_references_in_sim.py=
- =src/finmag/tests/bugs/test_bug_ndt_file_writing.py=
- =src/finmag/tests/test_time.py=
- =src/finmag/sim/hysteresis_test.py=
- =src/finmag/sim/magnetisation_patterns_test.py=
- =src/finmag/sim/sim_helpers_test.py= (full file)
- =src/finmag/sim/sim_test.py= (full file)
- =src/finmag/field_test.py=
- =src/finmag/field_setters_test.py=
- =src/finmag/scheduler/scheduler_test.py=
- =src/finmag/energies/exchange_test.py=
- =src/finmag/energies/anisotropy_test.py=
- =src/finmag/energies/zeeman_test.py=
- =src/finmag/physics/tests/test_effective_field.py=
- =src/finmag/physics/tests/test_equation.py=
- =src/finmag/physics/tests/test_terms.py=
- =src/finmag/energies/cubic_anisotropy_test.py=
- =src/finmag/energies/test_energies_in_regions.py=
- =src/finmag/energies/magnetostatic_field_test.py=
- =src/finmag/energies/thin_film_demag_test.py=
- =src/finmag/tests/test_energy_creation_with_variable_Ms.py=
- =src/finmag/tests/test_interactions_scale_linearly_with_m.py=
  - the weak-Krylov-demag case now has both:
    - a positive regression test documenting that loose demag solver
      tolerances are detectably non-linear, and
    - the original historical =xfail= case, kept intentionally so the
      tolerance expectations can be reviewed by a human later
- =src/finmag/tests/test_heun.py=
- =src/finmag/tests/test_spatially_varying_alpha.py=
- =src/finmag/tests/test_spatially_varying_anisotropy.py=
- =src/finmag/tests/test_unit_length.py=
- =src/finmag/tests/test_anis.py=
- =src/finmag/tests/test_dmi.py=
- =src/finmag/tests/test_dmi_terms.py=
- =src/finmag/util/fft_test.py=
- =src/finmag/energies/demag/demag_pbc_test.py=
- =src/finmag/energies/demag/fk_demag_test.py=
- =src/finmag/energies/demag/fk_demag_2d_test.py=
- =src/finmag/tests/demag/test_bem_computation.py=
- =src/finmag/tests/demag/test_demag_sphere.py=
- =src/finmag/energies/dmi_test.py=
- =src/finmag/util/fileio_test.py=
- =src/finmag/util/helpers_test.py=
- =src/finmag/util/length_scales_test.py=
- =src/finmag/util/meshes_test.py=
- =src/finmag/util/mesh_templates_test.py=
- =src/finmag/tests/test_meshes.py=
- =src/finmag/util/pbc_test.py=
- =src/finmag/util/plot_helpers_test.py=
- =src/finmag/util/vtk_saver_test.py=
- =src/finmag/util/dmi_helper_test.py=
- =src/finmag/util/test_set_function_values.py=
- =src/finmag/util/test_dmi_from_helix.py=
- =src/finmag/drivers/tests/sundials_nsteps_test.py=
- =src/finmag/drivers/tests/sundials_reinit_test.py=
- =src/finmag/drivers/tests/test_integrators.py=
- =src/finmag/drivers/tests/test_scipy.py=
- =src/finmag/tests/jacobean/test_jacobean_computation.py=
- =src/finmag/tests/jacobean/test_jacobean_integration.py=
- =src/finmag/tests/jacobean/test_native_llg.py=
- =src/finmag/util/ode/tests/test_sundials_stiff_ode.py=

Manual verification helper:

- =dev/bin/verify-python3-core-suite=

GitHub Actions transition workflow:

- =.github/workflows/python3-core-suite.yml=

** Netgen / Mesh-Test Findings

The transition image now also installs:

- =netgen=
- =tix=
- =xauth=
- =xvfb=

plus compatibility symlinks for the Tcl/Tix lookup paths expected by
=netgen 4.9.13=.

This fixes the earlier headless startup issue:

- =netgen= now starts under =xvfb-run=
- the Tcl/Tix startup failure is gone

But the historical binary still is not fully stable in this container:

- it can abort with =corrupted double-linked list= after successful mesh
  generation/export for some generated meshes

Operational consequence:

- tests should not treat =shutil.which("netgen")= as sufficient
- mesh-generation tests now use =finmag.util.meshes.netgen_is_usable()=
- =run_netgen()= now accepts the historical post-export abort when the target
  =.grid= file was written successfully
- mesh conversion now falls back to the Python module
  =dolfin_utils.meshconvert.meshconvert.convert2xml= when the
  =dolfin-convert= script is absent from =PATH=

Verified mesh-test result:

- =src/finmag/tests/test_meshes.py src/finmag/util/meshes_test.py=
- =12 passed, 3 skipped in 66.96s=

** External Package Brought Across: dolfinh5tools

From the repo structure and install scripts, =dolfinh5tools= is an external
package, not part of Finmag itself.

Evidence in-tree:

- install scripts and Docker recipes fetch it from
  =git+https://github.com/fangohr/dolfinh5tools.git=
- =src/finmag/field.py= imports it optionally and uses it for
  =Field.save_hdf5(...)=
- =src/finmag/field_test.py= currently uses =pytest.importorskip("dolfinh5tools")=
  for the HDF5 save test

Current Python 3 transition status:

- the transition image now installs =dolfinh5tools= directly from its GitHub
  repository during the Docker build
- the image applies a small Python 3 compatibility patch to the installed
  package:
  - explicit relative import in =dolfinh5tools/__init__.py=
  - Python 3 print syntax in =dolfinh5tools/lib.py=
- direct import now works under
  =docker run --rm -e OMPI_MCA_plm=isolated finmag-py3-dolfin2017 ...=
- =src/finmag/field_test.py::TestField::test_save_hdf5= now passes under
  Python 3
- because =src/finmag/field_test.py= was already part of
  =dev/bin/verify-python3-core-suite= and
  =.github/workflows/python3-core-suite.yml=, this progress is already
  reflected in CI without adding a new test file

One real runtime bug was fixed while bringing this suite across:

- =src/finmag/scheduler/derivedevents.py=

Specifically, =RepeatingTimeEvent= claimed to support callable intervals, but
its constructor still compared the interval directly against zero. Under
Python 3 this failed for =functools.partial= during =sim.relax()=. The fix was
to validate negativity only for non-callable intervals.

Additional Python 3/runtime fixes made while broadening the core suite:

- =src/finmag/sim/sim.py=: =dict.has_key= to membership test for VTK saver lookup
- =src/finmag/sim/sim_savers.py=: =dict.has_key= to membership test for field saver lookup
- =src/finmag/sim/magnetisation_patterns.py=: fix =xrange=, and avoid comparing
  NumPy arrays directly against the =\"singleCentre\"= sentinel string
- =src/finmag/util/meshes.py=: encode CSG text before =hashlib.md5(...)= under
  Python 3
- =src/finmag/sim/sim_helpers_test.py=: Python 3 imports/prints and a clean
  skip when =netgen= is unavailable for the nanodisk skyrmion subcase
- =src/finmag/sim/sim_test.py=: Python 3 print/xrange/iteritems cleanup,
  clean skips for =netgen=-dependent and =sllg=-dependent tests, and normal
  mode test path repaired
- =src/finmag/field.py=: add Python 3 =__truediv__= support so field
  normalisation and division tests work again
- =src/finmag/field_test.py=: use package import for =Field= and skip the
  HDF5 save test cleanly when =dolfinh5tools= is unavailable
- =src/finmag/scheduler/scheduler_test.py=: explicit package-relative imports
  for scheduler classes
- =src/finmag/energies/zeeman.py=: replace Python 2 =map(...)= assumptions
  with eager tuples for =dolfin.Constant=
- =src/finmag/energies/zeeman_test.py=: package import fix for
  =DipolarField=, Python 3 print/range cleanup, and clean skip for the
  =netgen=-dependent regional-energy test
- =src/finmag/energies/exchange_test.py=, =anisotropy_test.py=,
  =dmi_test.py=: Python 3 print cleanup
- =src/finmag/util/fileio_test.py=, =helpers_test.py=, =length_scales_test.py=,
  =meshes_test.py=, =mesh_templates_test.py=, =pbc_test.py=,
  =vtk_saver_test.py=, =dmi_helper_test.py=, =test_set_function_values.py=,
  =test_dmi_from_helix.py=: Python 3 import/syntax cleanup and explicit skips
  for missing external mesh tools
- =src/finmag/util/helpers.py=: decode subprocess output in
  =run_cmd_with_timeout()= for Python 3
- =src/finmag/util/mesh_templates.py=: encode CSG strings before hashing
- =src/finmag/util/DMI_from_helix.py=: Python 3 =range= and integer-division
  cleanup on the helix-analysis path
- =src/finmag/sim/magnetisation_patterns_test.py=: clean skip when =netgen=
  is unavailable in the transition image
- =src/finmag/sim/normal_mode_sim.py=: remove =past.builtins.basestring= and
  fix Python 3 array/None comparisons in the normal mode workflow
- =src/finmag/tests/jacobean/domain_wall_cobalt.py=: Python 3 cleanup for the
  shared domain-wall helper used by integrator tests
- =src/finmag/tests/jacobean/test_jacobean_integration.py= and related helper
  imports/prints/integer-division assumptions cleaned up for Python 3
- =src/finmag/tests/jacobean/test_jacobean_computation.py=: Python 3 relative
  import and integer-division cleanup for the Jacobian comparison coverage
- =src/finmag/tests/jacobean/test_native_llg.py=: Python 3 relative import and
  print cleanup for the native LLG coverage
- =src/finmag/util/time_counter.py=: Python 3 print cleanup required by the
  native LLG test import path
- =src/finmag/drivers/tests/sundials_reinit_test.py=: Python 3 print cleanup
  for the Sundials reinitialisation coverage
- =src/finmag/sim/sim_helpers.py=: Python 3-compatible loading/decoding of
  legacy Python 2 restart =.npz= files
- =src/finmag/util/fft.py=: final =xrange= cleanup on the normal mode export
  path
- =src/finmag/normal_modes/eigenmodes/*=: relative-import and Python 3 syntax
  cleanup so normal-mode support imports again
- =src/finmag/normal_modes/deprecated/normal_modes_deprecated.py=: Python 3
  iterator cleanup and relative import for custom colormaps

With the additional snapshot package:

- `libsundials-dev = 2.7.0+dfsg-2`

the Finmag native build in the Python 3 image now gets past missing-header
failures such as:

- `cvode/cvode.h`
- `nvector/nvector_serial.h`

The build now fails later, inside the Finmag Sundials wrapper itself.

Current failing file:

- `native/src/sundials/sundials_cvode_impl.h`

Observed issue:

- the wrapper only recognises Sundials `2.4.0` and `2.5.0`
- the snapshot image provides Sundials `2.7.0`
- the internal version-to-type mapping therefore resolves to:
  - `sundials_traits<-1>`

This causes the compile-time API mismatches now seen in the Python 3 image:

- callback signatures for `CVDlsSetDenseJacFn` and `CVDlsSetBandJacFn`
- `int*` vs `long int*` mismatches for:
  - `CVDlsGetLastFlag`
  - `CVDiagGetLastFlag`
  - `CVSpilsGetLastFlag`

Working assumption:

- Sundials `2.7.0` should be mapped like `2.5.0` here, i.e. use `long`
  parameter types in the CVODE wrapper
- the next patch should therefore start by extending the wrapper's version
  trait handling to `2.7.0`

** Native Build Result

The native blocker above has now been resolved sufficiently for the current
milestone.

Changes that proved necessary:

- extend the Sundials wrapper in:
  - `native/src/sundials/sundials_cvode_impl.h`
  to recognize Sundials `2.7.0`
- make the native build Python-version-aware in:
  - `native/Makefile`
  - `src/finmag/util/native_compiler.py`
- fix NumPy C-API initialization for Python 3 in:
  - `native/src/util/np_array.cc`
  - `native/src/util/np_array.h`
- restore expected math helper macros locally in:
  - `native/src/sundials/nvec_serial/nvector_serial_custom_malloc.c`

Observed result:

- the native modules now build and load far enough for:
  - `PYTHONPATH=/tmp/finmag/src python3 -c "import finmag"`
  to succeed in the Python 3 DOLFIN image

** Python 3 Import Milestone

The following command now succeeds:

#+begin_src bash
docker run --rm \
  -e OMPI_MCA_plm=isolated \
  -v /tank/fangohr/git/finmag:/repo:ro \
  finmag-py3-dolfin2017 \
  bash -lc 'rm -rf /tmp/finmag && cp -a /repo /tmp/finmag && cd /tmp/finmag && \
            PYTHONPATH=/tmp/finmag/src python3 -c "import finmag; print(finmag)"'
#+end_src

Observed output at the end of startup:

- module import succeeds
- `print(finmag)` reports:
  - `<module 'finmag' from '/tmp/finmag/src/finmag/__init__.py'>`

Additional observations from the successful import path:

- startup logging now reports:
  - `Python = 3.5.4`
  - `Dolfin = 2017.1.0`
  - `Sundials = 2.7.0`
- missing optional packages such as SciPy no longer abort the import
- `paraview` is absent in this image, which only triggers warnings

** Manual Verification Workflow

For manual verification of the current milestone, use the following steps from
the repository root.

*** 1. Build the Python 3 DOLFIN image

#+begin_src bash
docker build \
  -f install/docker/python3-dolfin2017-snapshot/Dockerfile \
  -t finmag-py3-dolfin2017 .
#+end_src

*** 2. Start an interactive container

#+begin_src bash
docker run --rm -it \
  -e OMPI_MCA_plm=isolated \
  -v "$PWD":/repo:ro \
  finmag-py3-dolfin2017 \
  bash
#+end_src

*** 3. Copy the checkout to a writable location and test the import

Inside the container:

#+begin_src bash
rm -rf /tmp/finmag
cp -a /repo /tmp/finmag
cd /tmp/finmag
PYTHONPATH=/tmp/finmag/src python3
#+end_src

Then at the Python prompt:

#+begin_src python
import finmag
print(finmag)
#+end_src

*** 4. One-shot non-interactive verification command

#+begin_src bash
docker run --rm \
  -e OMPI_MCA_plm=isolated \
  -v "$PWD":/repo:ro \
  finmag-py3-dolfin2017 \
  bash -lc 'rm -rf /tmp/finmag && cp -a /repo /tmp/finmag && cd /tmp/finmag && \
            PYTHONPATH=/tmp/finmag/src python3 -c "import finmag; print(finmag)"'
#+end_src

*** 5. Helper script

The repository now contains:

- `dev/bin/verify-python3-m1`
- `dev/bin/verify-python3-import`
- `dev/bin/verify-python3-barmini-smoke`
- `dev/bin/verify-python3-restart-smoke`
- `dev/bin/verify-python3-normal-modes-smoke`

This helper:

- builds the image
- runs the one-shot import verification command

It can be invoked as:

#+begin_src bash
dev/bin/verify-python3-m1
#+end_src

and:

#+begin_src bash
dev/bin/verify-python3-import
#+end_src

and:

#+begin_src bash
dev/bin/verify-python3-barmini-smoke
#+end_src

and:

#+begin_src bash
dev/bin/verify-python3-restart-smoke
#+end_src

and:

#+begin_src bash
dev/bin/verify-python3-normal-modes-smoke
#+end_src

** Scope-Limiting Import Decisions

To reach the import milestone without pulling in the entire unfinished Python 3
port, a few non-core features are now optional at import time:

- `SLLG` support is deferred unless the `sllg` kernel is explicitly selected
- the SciPy integrator backend is deferred unless explicitly selected
- normal-mode support is deferred unless explicitly selected

These are intentional scope boundaries for the current phase, not final
feature-complete Python 3 support. `SLLG` is no longer blocked, but keeping it
lazy at import time still avoids widening the base import surface unnecessarily.

** SLLG Path Recovered

The Python 3 transition now includes a working `sllg` execution path.

Changes made:

- `src/finmag/physics/llb/sllg.py`
  - integer division fix for `nxyz`
  - Python 3 exception syntax
  - Python 3 print cleanup in the module self-test block
  - native seed handling now passes a plain Python `int` instead of
    `numpy.int64` into the Boost.Python binding
- `src/finmag/tests/zhangli/zhang_li_test.py`
  - Python 3 print cleanup so the file collects again
- `src/finmag/physics/llb/sllg_test.py`
  - Python 3 parse cleanup for the auxiliary/random-number helper module
  - now contains a small pytest smoke test for the native
    `RandomMT19937.gaussian_random_np` path
- `src/finmag/physics/llb/llb.py`
  - Python 3 exception and manual-print cleanup so the module imports
  - `LLB.add(...)` now dispatches correctly between LLB's own exchange
    interaction and standard Finmag interactions such as Zeeman/Demag, passing
    a DG0 `Field` for `Ms` where those interactions expect one
  - `m_average_fun(...)` now uses the wrapped Dolfin function (`self._m.f`)
    when building UFL expressions
- `src/finmag/physics/llb/exchange.py`
  - Python 3 print cleanup in example/manual code paths
- `src/finmag/physics/llb/material.py`
  - Python 3 print cleanup in the module self-test block
- `src/finmag/physics/llb/llb_test.py`
  - Python 3 print cleanup so the file collects
  - removed the two stale expected failures; the save-data test now uses a
    two-layer z mesh so both saved regions have non-zero volume
- `src/finmag/tests/test_solid_angle.py`
  - Python 3 print cleanup in the manual runner
- `src/finmag/tests/test_solid_angle_invariance.py`
  - `xrange` to `range` cleanup
- `src/finmag/tests/energy_density/test_energy_density.py`
  - replaced Python 2 `commands.getstatusoutput` with
    `subprocess.getstatusoutput`
  - Python 3 print cleanup
- `src/finmag/tests/test_skyrmions.py`
  - no code changes required; added to the Python 3 gate after verification
- `src/finmag/tests/test_1d_domain_wall_profile_uniaxial_anisotropy.py`
  - Python 3 print cleanup
- `src/finmag/tests/cython/test_cython.py`
  - Python 3 print cleanup
  - uses `cython` or Debian's Python 3 executable name `cython3`, whichever is
    available
- `src/finmag/physics/tests/neb/neb_test.py`
  - no code changes required; added to the Python 3 gate after verification
- `src/finmag/physics/llg_stt.py`
  - integer division fixes for array/node counts
- `src/finmag/tests/zhangli/stt_nonlocal_test.py`
  - Python 3 print cleanup
- `src/finmag/util/oommf/init.py`
  - relative imports and Python 3 print cleanup
- `src/finmag/util/oommf/mesh.py`
  - `xrange` to `range` cleanup
- `src/finmag/util/oommf/oommf_calculator.py`
  - Python 3 import/exception/print cleanup sufficient for package import
- `src/finmag/util/oommf/ovf.py`
  - relative lattice import and Python 3 dictionary/print cleanup
- `src/finmag/util/oommf/test_mesh.py`
  - no test changes required; added after the utility package could import
- `src/finmag/util/visualization.py`
  - optional `sh` and `IPython` imports so pure helpers can be used without
    GUI/rendering dependencies
  - relative `visualization_impl` imports
- `src/finmag/util/visualization_impl.py`
  - optional `sh` and `IPython` imports, `xrange` to `range`, and Python 3
    print cleanup
- `src/finmag/tests/test_sim_parallel.py`
  - Python 3 print cleanup
  - the parallel test now collects explicitly and skips in serial runs with
    fewer than two MPI ranks
- `src/finmag/tests/comparison/test_dmdt.py`
  - Python 3 print cleanup
  - OOMMF-dependent test now skips explicitly only when the `oommf` executable
    is unavailable; it runs in the transition image
- `src/finmag/tests/nmag/exchange_1d/test_exchange_1d.py`
  - Python 3 print cleanup
  - `xrange` to `range` cleanup
- `src/finmag/tests/nmag/anisotropy_1d/test_nmag_1d_anisotropy.py`
  - Python 3 print cleanup
- `src/finmag/tests/nmag/spinwaves/run_finmag.py`
  - `xrange` to `range` cleanup
- `src/finmag/tests/nmag/spinwaves/test_spinwaves.py`
  - Python 3 print cleanup
- `src/finmag/tests/nmag/exchange_3d/test_dynamics_3D.py`
  - Python 3 print cleanup in the profiling helper block
- `src/finmag/tests/oommf/test_anisotropy.py`
  - Python 3 print cleanup
  - skips explicitly when the `oommf` executable is unavailable
- `src/finmag/tests/oommf/test_exchange.py`
  - Python 3 print cleanup
  - skips explicitly when the `oommf` executable is unavailable
- `src/finmag/tests/slonczewski/validation/finmag/test_finmag_validation.py`
  - Python 3 print cleanup in the plotting helper
- `src/finmag/tests/slonczewski/oscillator/run.py`
  - Python 3 print cleanup
- `src/finmag/tests/slonczewski/oscillator/test_oscillator.py`
  - renamed the previously hidden `_test_oscillator` to a collected test
  - marks the long oscillator validation as skipped for the current core gate
  - moved the expensive `run` import inside the skipped test to avoid mesh
    conversion during collection

Targeted verification succeeded for:

- `src/finmag/sim/sim_test.py::TestSimulation::test_sim_sllg`
- `src/finmag/sim/sim_test.py::TestSimulation::test_sim_sllg_time`
- `src/finmag/tests/zhangli/zhang_li_test.py`
  - result: `2 passed` for the file-level Zhang-Li tests under Python 3
- `src/finmag/physics/llb/sllg_test.py`
  - result: `1 passed` under Python 3
- `src/finmag/physics/llb/llb_test.py`
  - result: `2 passed` under Python 3
- `src/finmag/tests/test_solid_angle.py`
- `src/finmag/tests/test_solid_angle_invariance.py`
  - result: `6 passed` for the two-file solid-angle subset under Python 3
- `src/finmag/tests/energy_density/test_energy_density.py`
  - result: external nmag comparison skipped when `nsim` is absent; remaining
    energy-density tests passed under Python 3
- `src/finmag/tests/test_skyrmions.py`
  - result: `1 passed` under Python 3
- `src/finmag/tests/test_1d_domain_wall_profile_uniaxial_anisotropy.py`
  - result: `1 passed` under Python 3; one existing runtime warning from the
    curve-fit path
- `src/finmag/tests/cython/test_cython.py`
  - result: `1 passed` under Python 3 using `/usr/bin/cython3`
- `src/finmag/physics/tests/neb/neb_test.py`
  - result: `2 passed` under Python 3
- `src/finmag/tests/zhangli/stt_nonlocal_test.py`
  - result: `1 passed` under Python 3
- `src/finmag/util/oommf/test_mesh.py`
  - result: `3 passed` under Python 3
- `src/finmag/util/visualization_test.py`
  - result: pure flight-path tests passed; GUI/rendering tests remained
    skipped as before
- `src/finmag/sim/sim_test.py::test_compute_eigenmode_animations`
  - result: passed the non-movie VTK eigenmode animation export path under
    Python 3
- `src/finmag/sim/sim_test.py::test_schedule_render_scene`
- `src/finmag/sim/sim_test.py::test_output_formats_for_exporting_normal_mode_animations`
- `src/finmag/util/helpers_test.py::test_jpg2avi`
- `src/finmag/util/helpers_test.py::test_pvd2avi`
  - result: skipped explicitly because the transition image does not provide
    Paraview rendering, `sh`, and/or `mencoder`
- `src/finmag/util/helpers_test.py::test_get_hg_revision_info`
  - result: skipped explicitly because the transition image does not provide
    Mercurial; this is historical pre-Git coverage and not a transition
    dependency
- Combined targeted verification for the files added in this pass:
  - `src/finmag/tests/test_skyrmions.py`
  - `src/finmag/tests/energy_density/test_energy_density.py`
  - `src/finmag/tests/test_1d_domain_wall_profile_uniaxial_anisotropy.py`
  - `src/finmag/tests/cython/test_cython.py`
  - `src/finmag/physics/tests/neb/neb_test.py`
  - `src/finmag/tests/zhangli/stt_nonlocal_test.py`
  - `src/finmag/util/oommf/test_mesh.py`
  - `src/finmag/util/visualization_test.py`
  - result: command exited successfully under Python 3; expected skips remain
    for missing/external optional tools
- `src/finmag/tests/test_sim_parallel.py`
  - result: skipped in the serial transition-container run because it requires
    at least two MPI ranks
- `src/finmag/tests/comparison/test_dmdt.py`
  - result: passes in the transition image now that `oommf` is installed
- `src/finmag/tests/nmag/exchange_1d/test_exchange_1d.py`
  - result: `4 passed` under Python 3 against checked-in reference data; one
    existing divide-by-zero/NaN warning in the relative-difference diagnostic
- `src/finmag/tests/nmag/anisotropy_1d/test_nmag_1d_anisotropy.py`
  - result: `3 passed` under Python 3 against checked-in reference data
- `src/finmag/tests/nmag/spinwaves/test_spinwaves.py`
  - result: `1 passed` under Python 3 against checked-in reference data
- `src/finmag/tests/nmag/exchange_3d/test_dynamics_3D.py`
  - result: `1 passed` under Python 3 against checked-in reference data
- `src/finmag/tests/oommf/test_anisotropy.py`
- `src/finmag/tests/oommf/test_exchange.py`
  - result: pass in the transition image now that `oommf` is installed
- `src/finmag/tests/slonczewski/validation/finmag/test_finmag_validation.py`
  - result: `1 passed` under Python 3; this is a comparatively slow
    simulation test because it runs to `10e-9` seconds
- `src/finmag/tests/slonczewski/oscillator/test_oscillator.py`
  - result: skipped in the transition image by explicit marker

The active Python 3 gate now includes:

- `src/finmag/tests/zhangli/zhang_li_test.py`
- `src/finmag/physics/llb/sllg_test.py`
- `src/finmag/physics/llb/llb_test.py`
- `src/finmag/tests/test_solid_angle.py`
- `src/finmag/tests/test_solid_angle_invariance.py`
- `src/finmag/tests/energy_density/test_energy_density.py`
- `src/finmag/tests/test_skyrmions.py`
- `src/finmag/tests/test_1d_domain_wall_profile_uniaxial_anisotropy.py`
- `src/finmag/tests/cython/test_cython.py`
- `src/finmag/physics/tests/neb/neb_test.py`
- `src/finmag/tests/zhangli/stt_nonlocal_test.py`
- `src/finmag/util/oommf/test_mesh.py`
- `src/finmag/util/visualization_test.py`
- `src/finmag/tests/test_sim_parallel.py`
- `src/finmag/tests/comparison/test_dmdt.py`
- `src/finmag/tests/oommf/test_anisotropy.py`
- `src/finmag/tests/oommf/test_exchange.py`
- `src/finmag/tests/nmag/anisotropy_1d/test_nmag_1d_anisotropy.py`
- `src/finmag/tests/nmag/exchange_1d/test_exchange_1d.py`
- `src/finmag/tests/nmag/exchange_3d/test_dynamics_3D.py`
- `src/finmag/tests/nmag/spinwaves/test_spinwaves.py`
- `src/finmag/tests/slonczewski/validation/finmag/test_finmag_validation.py`
- `src/finmag/tests/slonczewski/oscillator/test_oscillator.py`

** Gmsh Polygon Helpers Recovered

The Python 3 transition image now includes `gmsh`, and the two
`util/meshes_test.py` polygon helper tests no longer depend on the
`dolfin-convert` script being installed as a standalone executable.

Changes made:

- `install/docker/python3-dolfin2017-snapshot/Dockerfile`
  - now installs `gmsh`
- `src/finmag/util/meshes.py`
  - added a small shared `gmsh -> meshconvert -> df.Mesh(...)` helper
  - switched `regular_polygon(...)` and `regular_polygon_extruded(...)` to use
    the existing Python meshconvert fallback instead of shelling out to
    `dolfin-convert` directly
  - fixed the legacy `.geo` syntax from `Physical Surface = {1};` to
    `Physical Surface(1) = {1};` for `gmsh 2.15`
- `src/finmag/util/meshes_test.py`
  - the tests now require only `gmsh`, not `gmsh` plus a separate
    `dolfin-convert` executable
  - `test_build_mesh` no longer imports `mshr`; it uses Dolfin-native
    rectangle, disk, and box meshes, so it is active in the transition image
- `src/finmag/sim/sim_test.py`
  - `test_m_average_is_robust_with_respect_to_mesh_discretization` now uses
    `subprocess` with `gmsh` and the shared Python meshconvert fallback
  - this removes the Python-2-era `sh` dependency and avoids requiring a
    standalone `dolfin-convert` executable
- `src/finmag/normal_modes/eigenmodes/helpers_test.py`
- `src/finmag/normal_modes/eigenmodes/eigenproblems_test.py`
- `src/finmag/normal_modes/eigenmodes/eigensolvers_test.py`
- `src/finmag/normal_modes/deprecated/normal_modes_deprecated_test.py`
  - updated Python 2 print/range idioms
  - changed local imports to package-relative imports
  - fixed the dense eigensolver `num=None` path, which Python 3 rejected
  - removed unconditional SLEPc object/debug dumps from the solver loop
  - fixed Python 3 iterator semantics in `compute_normal_modes_generalised`
  - added the files to the local verifier and GitHub Actions core-suite gate

Targeted verification succeeded for:

- `src/finmag/util/meshes_test.py::test_regular_polygon`
- `src/finmag/util/meshes_test.py::test_regular_polygon_extruded`
- `src/finmag/util/meshes_test.py::test_build_mesh`
- `src/finmag/util/meshes_test.py`
- `src/finmag/sim/sim_test.py::test_m_average_is_robust_with_respect_to_mesh_discretization`
- `src/finmag/normal_modes/eigenmodes/helpers_test.py`
- `src/finmag/normal_modes/eigenmodes/eigenproblems_test.py`
- `src/finmag/normal_modes/eigenmodes/eigensolvers_test.py`
- `src/finmag/normal_modes/deprecated/normal_modes_deprecated_test.py`
  - result: targeted tests passed

Full CI-equivalent local verification also succeeded with:

- `dev/bin/verify-python3-core-suite`
- result: `397 passed, 8 skipped, 1 xfailed`
- note: this full-suite result predates the latest normal-modes gate expansion;
  the expanded normal-modes subset has been verified separately with its
  existing expected xfails/skips

** GCR Demag Remains Unported

The historical `GCR` demag solver is intentionally not counted as ported on
the Python 3 path.

What changed:

- `src/finmag/energies/demag/gcr_demag.py`
  - now serves only as a historical placeholder
  - instantiating `GCRDemag` raises `NotImplementedError`
- `src/finmag/energies/demag/__init__.py`
  - `"GCR"` was removed from `KNOWN_SOLVERS`

Why:

- the repo still contains the native `compute_bem_gcr(...)` symbol
- but the surviving Python-side demag code does not retain a complete and
  clearly correct GCR formulation
- mapping `Demag("GCR")` to FK made the tests pass but was scientifically
  misleading, since it did not restore a distinct GCR solve

Current status:

- `test_compute_scalar_potential_gcr` is an explicit skip again
- the Python 3 transition focuses on genuinely ported functionality only

** Next Transition Step

The next concrete validation target is:

1. create a minimal `Simulation` under Python 3 in `finmag-py3-dolfin2017`
2. run a small `barmini`-based smoke path
3. then begin bringing the agreed minimal acceptance suite across

** Barmini Smoke Milestone

The runtime milestone above has now been reached.

This remains the primary practical smoke path for M1 because it exercises the
time-integration workflow directly.

The following command succeeds:

#+begin_src bash
docker run --rm \
  -e OMPI_MCA_plm=isolated \
  -v /tank/fangohr/git/finmag:/repo:ro \
  finmag-py3-dolfin2017 \
  bash -lc 'rm -rf /tmp/finmag && cp -a /repo /tmp/finmag && cd /tmp/finmag && \
            PYTHONPATH=/tmp/finmag/src python3 - <<\"PY\"
import finmag
sim = finmag.example.barmini(name=\"barmini_py3_smoke\")
print(\"constructed\", sim.__class__.__name__)
sim.run_until(1e-12)
print(\"advanced\", sim.t)
PY'
#+end_src

Observed result:

- `constructed Simulation`
- `advanced 1e-12`

** Restart Smoke Milestone

The M1 workflow matrix now also has an explicit restart/save-load smoke command
built on top of `barmini`.

The following helper succeeds:

#+begin_src bash
dev/bin/verify-python3-restart-smoke
#+end_src

Observed result:

- `saved_time 1e-11`
- `restarted_time 1e-11`
- `copied_time 1e-11`
- the canonical restart file `barmini_restart_py3_smoke-restart.npz` is created

This verifies the practical restart path in three steps:

- save restart data from a running `barmini` simulation;
- restart the same simulation object from the canonical file;
- create a fresh `barmini` simulation object and restart that from the same
  saved state.

** Aggregate M1 Verification Command

The practical M1 witness can also be run as one command:

#+begin_src bash
dev/bin/verify-python3-m1
#+end_src

This wrapper runs the four manual M1 workflow witnesses in sequence:

- import
- `barmini` time integration
- `barmini` restart/save-load
- supplementary normal-modes smoke coverage

This is intended as the simplest reproducible answer to "is M1 usable right
now?" without requiring a full pytest run.

The helper now executes the full M1 witness in a single container session and
single writable checkout copy, which avoids repeating the native rebuild four
times. The targeted sub-helpers still exist for manual debugging, and GitHub
Actions has a matching lightweight workflow in:

- `.github/workflows/python3-m1.yml`

** Normal-Modes Smoke Milestone

The M1 workflow matrix also has an explicit normal-modes smoke command, but it
is supplementary rather than primary. For practical usability on the legacy
stack, the =barmini= time-integration path is the more important smoke witness.

The following helper succeeds:

#+begin_src bash
dev/bin/verify-python3-normal-modes-smoke
#+end_src

Observed result:

- `constructed NormalModeSimulation`
- `modes 2`
- a finite printed `first_freq`
- a small printed `first_rel_error`

This smoke path is intentionally lightweight. It reuses the checked-in nanodisk
mesh and relaxed restart state from `src/finmag/example/normal_modes/`, so it
verifies the Python 3 normal-modes runtime without paying the cost of fresh
mesh generation or relaxation.

** What Fixed The Runtime Regression

After `import finmag` was working, the next blocker was a CVODE runtime failure
even for trivial ODEs under Python 3:

- `CV_CONV_FAILURE` at `t = 0`

Important narrowing observations:

- the same `barmini().run_until(1e-12)` step succeeds in the Python 2 reference image
- the initial Finmag RHS for `barmini` is identical in Python 2 and Python 3
- even a one-dimensional test problem `y' = 0.5 y` failed in the Python 3 image

This showed that the regression was in the native Sundials/NVector runtime
path, not in the Python-level field setup.

The fix that restored runtime integration was:

- stop linking the Python 3 Sundials module against Finmag's vendored custom
  `libnvector_serial_custom_malloc`
- link it against the system:
  - `libsundials_nvecserial`
- construct callback-local NumPy array views directly from raw NVector memory in:
  - `native/src/sundials/numpy_malloc.cc`

This keeps the callback bridge working under Python 3 while avoiding the
runtime incompatibility between the old vendored NVector implementation and
SUNDIALS `2.7.0`.

** Updated Next Step

The next concrete validation target is now:

1. bring across the `barmini`-based subset of the agreed minimal acceptance suite
2. then broaden to the rest of the minimal suite under Python 3

** Related Repo Files

These files were added to support this transition work:

- `dev/bin/check-python3-in-container`
- `dev/bin/verify-python3-import`
- `dev/bin/verify-python3-barmini-smoke`
- `install/docker/python3-bootstrap/Dockerfile`
- `install/docker/python3-bootstrap/README.md`
- `install/docker/python3-dolfin2017-snapshot/Dockerfile`
- `install/docker/python3-dolfin2017-snapshot/README.md`

** Comparison Test Milestone

The remaining legacy comparison test files under
=src/finmag/tests/comparison/= now collect under Python 3 and are represented
in the active core gate.

Useful details:

- =finmag.util.magpar.read_inp= now reads gzipped reference data in text mode,
  which is required for Python 3.
- =finmag.util.oommf.comparison= now materialises =zip(...)= iterators before
  building NumPy coordinate arrays.
- =finmag.util.oommf.oommf_calculator= and =finmag.util.oommf.ovf= were patched
  far enough that OOMMF comparison tests run under Python 3.
- =finmag.util.oommf.lattice= imports =reduce= from =functools=.
- OOMMF is now installed in the transition image, so those comparison tests run
  as real regression tests there.
- Two saved Magpar mesh comparisons are explicit expected failures because
  their checked-in reference meshes do not match meshes regenerated by the
  current Netgen path.

The targeted comparison subset exits successfully in the transition container:

#+begin_src bash
PYTHONPATH=/tmp/finmag/src pytest -q \
  src/finmag/tests/comparison/anisotropy/test_anis_magpar.py \
  src/finmag/tests/comparison/anisotropy/test_anis_oommf.py \
  src/finmag/tests/comparison/anisotropy/test_cubic_anis_oommf.py \
  src/finmag/tests/comparison/demag/test_demag_field.py \
  src/finmag/tests/comparison/exchange/test_exchange_compare_magpar.py \
  src/finmag/tests/comparison/exchange/test_exchange_field.py
#+end_src

** Full Python 3 Core Gate Result

The current Python 3 core verifier completed successfully in the transition
image.

Command:

#+begin_src bash
dev/bin/verify-python3-core-suite
#+end_src

Result:

- =502 passed=
- =14 skipped=
- =3 xfailed=
- runtime: approximately =1547.35s=

The skip/xfail count is intentional at this stage:

- external tools such as =nsim=, =paraview=, GUI/rendering paths, Mercurial,
  and MPI multi-rank execution are skipped when absent from the transition
  image
- legacy GCR demag and stale Magpar reference-mesh comparisons are tracked as
  expected failures/skips rather than silently ignored

The gate now provides broad Python 3 regression coverage across field helpers,
mesh helpers, comparison data, normal modes, drivers, Sundials, demag/BEM,
SLLG, Zhang-Li, simulation helpers and the main =Simulation= subsystem.

Follow-up targeted verification after this baseline:

- =src/finmag/tests/test_interactions_scale_linearly_with_m.py= keeps the
  historical weak-Krylov-demag =xfail= for human review later, but now also
  has an explicit passing regression test showing that loose Krylov tolerances
  are detectably non-linear
- =src/finmag/normal_modes/deprecated/normal_modes_deprecated_test.py::
  test_plot_spatially_resolved_normal_mode_in_region= is no longer a stale
  =xfail=; it passes under Python 3 after removing the obsolete
  =use_fenicstools= keyword from the test
- =src/finmag/normal_modes/eigenmodes/eigensolvers_test.py= no longer keeps
  the stale SciPy-sparse/Nanostrip =xfail= for =N=200, dtype=float=; that
  case now passes
- =src/finmag/normal_modes/eigenmodes/eigensolvers.py= now correctly forwards
  configured =tol= and =maxit= into the SLEPc solve call instead of
  accidentally overriding them with truthy default arguments
- =src/finmag/normal_modes/eigenmodes/eigensolvers.py= now also promotes
  Hermitian inputs from generic SLEPc =NHEP=/=GNHEP= declarations to the
  stronger =HEP=/=GHEP= solver classes automatically; this removes the former
  RingGraph =N=200 SLEPc failure in =eigensolvers_test.py=
- =src/finmag/energies/demag/fk_demag_test.py::test_thin_film_argument_saves_time_on_thin_film=
  now allows a small regression margin instead of asserting strict
  wall-clock ordering, which removes a flaky CI/container timing failure
- plotting-heavy tests now close returned matplotlib figures explicitly in
  =eigenproblems_test.py=, =normal_modes_deprecated_test.py=,
  =sim_test.py=, and =plot_helpers_test.py=; this removes the repeated
  "More than 20 figures have been opened" warnings from the core-suite run
- =src/finmag/normal_modes/deprecated/normal_modes_deprecated.py=
  no longer forces matplotlib =figure.autolayout=True= inside
  =plot_spatially_resolved_normal_mode()=; that removes the old
  =tight_layout= compatibility warnings emitted while saving those figures
- =src/finmag/normal_modes/eigenmodes/helpers.py::compute_relative_error()=
  now handles a zero denominator explicitly, so the zero-eigenvalue path in
  =eigensolvers_test.py= no longer emits divide-by-zero warnings
- =export_normal_mode_animation()= now treats =freq ~= 0= as a zero-length
  animation window, which removes the divide-by-zero warnings from the
  eigenmode-animation smoke tests
- =src/finmag/energies/exchange_test.py= and
  =src/finmag/tests/nmag/exchange_1d/test_exchange_1d.py= now use masked
  =np.divide(..., where=...)= for relative-difference diagnostics, which
  removes warnings caused only by exact-zero reference values
- =src/finmag/util/helpers.py::fnormalise()= now uses masked division for
  zero vectors, preserving the historical result without emitting the old
  invalid-divide warning
- =test_domain_wall_profile= now suppresses the exploratory =sqrt(A / K1)=
  warning emitted while =curve_fit()= probes unphysical intermediate
  parameters, without changing the original fit behavior
- =eigensolvers_test.py= now suppresses the SciPy sparse/Nanostrip
  singular-matrix warning locally in the known shift-invert testcase, because
  SciPy still returns valid eigenpairs there
- =test_robertson_scipy_transposed= now asserts the expected SciPy/VODE
  "Excess work done" warning explicitly instead of letting it leak into the
  suite warning summary
- the GitHub Actions =python3-core-suite= workflow already includes the full
  deprecated normal-modes test file, so this progress is covered by CI without
  further workflow changes
- the current full rerun of =dev/bin/verify-python3-core-suite= confirms the
  post-cleanup baseline above

The current pytest core-gate baseline is warning-clean. The old DVODE Fortran
diagnostic still prints to stderr at process shutdown for the pathological
Robertson/VODE case, but it is no longer a pytest warning.

Remaining expected failures in the current baseline:

- =src/finmag/tests/comparison/anisotropy/test_anis_magpar.py::test_against_magpar=
  fails before tolerance comparison because the regenerated Netgen node array
  no longer has the same shape as the saved Magpar reference nodes
- =src/finmag/tests/comparison/demag/test_demag_field.py::test_using_magpar=
  fails for the same reason: saved Magpar reference nodes do not match the
  regenerated Netgen mesh, so =compare_field_directly()= aborts on node-array
  shape mismatch
- =src/finmag/tests/test_interactions_scale_linearly_with_m.py= intentionally
  keeps the historical weak-Krylov-demag =xfail= for later human review,
  alongside the new explicit negative regression test

** Current M3 Pixi/FEniCS-2019 Baseline

The current M3 verifier now completes successfully with a wider finite subset
of the legacy regression suite under =pixi= and conda-forge =fenics 2019.1.0=.

Verified command:

#+begin_src bash
dev/bin/verify-python3-m3
#+end_src

Observed result:

- =493 passed=
- =21 skipped=
- =5 xfailed=
- runtime approximately =730s=

This increment promoted the remaining checked-reference comparison and
single-rank-safe boundary files that do not require installing =nsim=:

- =src/finmag/tests/comparison/anisotropy/test_anis_magpar.py=
- =src/finmag/tests/comparison/demag/test_demag_field.py=
- =src/finmag/tests/comparison/exchange/test_exchange_compare_magpar.py=
- =src/finmag/tests/test_sim_parallel.py=
- =src/finmag/tests/nmag/anisotropy_1d/test_nmag_1d_anisotropy.py=
- =src/finmag/tests/nmag/exchange_1d/test_exchange_1d.py=
- =src/finmag/tests/nmag/exchange_3d/test_dynamics_3D.py=
- =src/finmag/tests/nmag/spinwaves/test_spinwaves.py=
- =src/finmag/tests/slonczewski/validation/finmag/test_finmag_validation.py=
- =src/finmag/tests/slonczewski/oscillator/test_oscillator.py=
- =src/finmag/energies/demag/demag_pbc_test.py=
- =src/finmag/normal_modes/deprecated/normal_modes_deprecated_test.py=

The focused promoted subset passes as =12 passed, 2 skipped, 2 xfailed=, and
the full expanded =dev/bin/verify-python3-m3= verifier exits successfully.
The two new xfails are the existing Magpar mesh-drift comparisons; the skips
are the single-rank MPI boundary and the intentionally long Slonczewski
oscillator validation. [Codex gpt-5.5 high]

=src/finmag/energies/demag/demag_pbc_test.py= is also included in the M3 gate
now. Because =finmag.native.treecode_bem= is not available on the pixi native
build, it is collected as =2 skipped=. The file now uses a module-level
=skipif= marker rather than module-level =importorskip= so focused runs exit
successfully instead of returning pytest's no-tests-collected code. [Codex
gpt-5.5 high]

The deprecated normal-mode regression file is now included as well. The
Kittel sphere failure was traced to ARPACK's implicit random start vector in
SciPy =1.17.1=; =compute_normal_modes_generalised()= now supplies a
deterministic alternating =v0= when callers do not provide one. The focused
file passes as =5 passed, 1 skipped=, and the pixi/M3 suite now covers all
=101= files listed by =dev/bin/verify-python3-core-suite=. [Codex gpt-5.5
high]

The previous increment promoted =oommf= from conda-forge into the pixi environment
and added real OOMMF/reference-data comparisons to the M3 gate:

- =src/finmag/tests/oommf/test_anisotropy.py=
- =src/finmag/tests/oommf/test_exchange.py=
- =src/finmag/tests/comparison/test_dmdt.py=
- =src/finmag/tests/comparison/anisotropy/test_anis_oommf.py=
- =src/finmag/tests/comparison/anisotropy/test_cubic_anis_oommf.py=
- =src/finmag/tests/comparison/exchange/test_exchange_field.py=

The focused promoted subset passes as =9 passed=, and the full
=dev/bin/verify-python3-m3= verifier exits successfully with these files
included. The exchange comparison still does not install or execute =nsim=:
it uses the checked-in Nmag reference field data and therefore remains useful
on the pixi track without the hard-to-install Nmag toolchain. [Codex GPT-5.4]

The previous increment promoted:

- =src/finmag/energies/zeeman_test.py=
- =src/finmag/tests/demag/test_bem_computation.py=
- =src/finmag/tests/demag/test_demag_sphere.py=

The Zeeman promotion keeps the historical sphere-vs-dipole absolute
stray-field guard at =140.0= A/m. Investigation showed that the
DOLFIN-2019/Netgen mesh with =maxh_sphere= = =2.5= produced a slightly larger
pointwise tail (about =150.9= A/m) despite passing the relative field checks;
refining only the magnetic sphere to =maxh_sphere= = =2.0= brings the tail
back below the original guard and avoids hiding the drift by loosening the
assertion. [Codex GPT-5.4]

The same increment promotes the non-PBC demag/BEM regression files. The
Magpar boundary-element reference helper now falls back to a pure-NumPy
implementation when =instant= is absent, and the BEM tests use the production
DOLFIN-2019 array fallback for native FK BEM construction. The legacy GCR
solver path remains intentionally skipped on Python 3. [Codex GPT-5.4]

An earlier blocker was the removed =dolfin.compile_extension_module=
entry point on the =pixi= /=fenics2019= path. The active workaround is a
small Python fallback for the low-level =Equation=/=terms= wrapper, while
the legacy Docker path still uses the compiled implementation.

Follow-up backend-confidence checks are now in place as well:

- native-vs-Python parity tests for the low-level =terms= wrapper
- native-vs-Python parity tests for =Equation.solve()= and pinning
- a Python-fallback =jtimes= test checked against centered finite differences
- checked-in native reference data in
  =src/finmag/physics/tests/equation_reference_data.json= so the fallback can
  still be validated numerically after the compiled backend is no longer
  runnable in active environments
- a benchmark-report test on the legacy/native stack; the current observed
  ratio is roughly =107x= slower for the Python fallback on a small repeated
  =Equation.solve()= loop

Newly promoted tests in this increment:

- =src/finmag/tests/zhangli/zhang_li_test.py=
- =src/finmag/tests/zhangli/stt_nonlocal_test.py=
- =src/finmag/tests/test_skyrmions.py=
- =src/finmag/tests/test_solid_angle.py=
- =src/finmag/tests/test_solid_angle_invariance.py=
- =src/finmag/physics/tests/neb/neb_test.py=
- =src/finmag/physics/llb/sllg_test.py=
- =src/finmag/physics/llb/llb_test.py=
- =src/finmag/util/meshes_test.py=
- =src/finmag/sim/sim_helpers_test.py=
- =src/finmag/sim/magnetisation_patterns_test.py=
- =src/finmag/sim/hysteresis_test.py=
- =src/finmag/tests/test_meshes.py=
- =src/finmag/util/mesh_templates_test.py=
- =src/finmag/util/helpers_test.py=
- =src/finmag/util/length_scales_test.py=
- =src/finmag/util/pbc_test.py=
- =src/finmag/util/plot_helpers_test.py=
- =src/finmag/util/vtk_saver_test.py=
- =src/finmag/util/fileio_test.py=
- =src/finmag/util/dmi_helper_test.py=
- =src/finmag/util/test_set_function_values.py=
- =src/finmag/util/test_dmi_from_helix.py=
- =src/finmag/drivers/tests/sundials_nsteps_test.py=
- =src/finmag/drivers/tests/sundials_reinit_test.py=
- =src/finmag/drivers/tests/test_scipy.py=
- =src/finmag/drivers/tests/test_integrators.py=
- =src/finmag/util/ode/tests/test_sundials_stiff_ode.py=
- =src/finmag/tests/jacobean/test_jacobean_computation.py=
- =src/finmag/tests/jacobean/test_jacobean_integration.py=
- =src/finmag/tests/jacobean/test_native_llg.py=
- =src/finmag/util/oommf/test_mesh.py=
- =src/finmag/util/visualization_test.py=
- =src/finmag/util/fft_test.py=
- =src/finmag/energies/dmi_test.py=
- =src/finmag/energies/magnetostatic_field_test.py=
- =src/finmag/energies/zeeman_test.py=
- =src/finmag/tests/demag/test_bem_computation.py=
- =src/finmag/tests/demag/test_demag_sphere.py=
- =src/finmag/drivers/tests/test_relaxation.py=
- =src/finmag/drivers/tests/test_relax_two_times.py=
- =src/finmag/tests/test_time.py=
- =src/finmag/scheduler/scheduler_test.py=
- =src/finmag/tests/test_cyclic_references_in_sim.py=
- =src/finmag/tests/bugs/test_bug_ndt_file_writing.py=
- =src/finmag/tests/cython/test_cython.py=
- =src/finmag/physics/tests/test_effective_field.py=
- =src/finmag/tests/test_jacobian.py=
- =src/finmag/normal_modes/eigenmodes/helpers_test.py=
- =src/finmag/normal_modes/eigenmodes/eigenproblems_test.py=
- =src/finmag/normal_modes/eigenmodes/eigensolvers_test.py=
- =src/finmag/field_setters_test.py=
- =src/finmag/field_test.py=
- =src/finmag/tests/oommf/test_anisotropy.py=
- =src/finmag/tests/oommf/test_exchange.py=
- =src/finmag/tests/comparison/test_dmdt.py=
- =src/finmag/tests/comparison/anisotropy/test_anis_oommf.py=
- =src/finmag/tests/comparison/anisotropy/test_cubic_anis_oommf.py=
- =src/finmag/tests/comparison/exchange/test_exchange_field.py=

Compatibility changes that enabled this:

- =finmag.util.helpers.vector_valued_function()= and
  =scalar_valued_function()= now accept =df.UserExpression= and initialize
  callable helper expressions in the DOLFIN 2019-compatible order, which
  repairs callable magnetisation/current-profile setup on the pixi path
- =finmag.physics.llg_stt.LLG_STT= now uses =get_local()= instead of the
  removed PETSc vector =.array()= accessor in the exercised Sundials/STT path
- =test_zhangli_sllg= now skips explicitly when =finmag.native.llb= is absent,
  which keeps the deterministic Zhang-Li regression in M3 without forcing the
  stochastic =sllg= native module into the first pixi build
- =finmag.util.solid_angle_magpar.return_csa_magpar()= now falls back to a
  NumPy implementation of the Magpar solid-angle reference formula when the
  old =instant= package is unavailable on the pixi path
- =finmag.physics.neb= now imports without =finmag.native.neb= for helper-only
  use, and defers the native-module requirement to the actual tangent
  computation path used by full NEB runs
- the pixi native build now includes =llb.so= again, and
  =finmag.physics.llb.sllg.SLLG= now uses =get_local()= on the DOLFIN 2019
  PETSc-vector paths exercised by the stochastic =sllg= runtime
- =finmag.util.meshes.build_mesh()= now opens =MeshEditor= with the DOLFIN
  2019 cell-type string API, while the Gmsh helper forces =-format msh2= so
  the legacy =dolfin-convert= path can still import the generated mesh on the
  pixi stack
- =finmag.sim.sim_savers= now saves PETSc-backed field data through
  =vector().get_local()=, and the skyrmion-density helper in
  =finmag.sim.sim_helpers= uses the same DOLFIN 2019-compatible vector access
- =finmag.normal_modes.eigenmodes.helpers= now passes =rcond=None= into
  =numpy.linalg.lstsq()= and uses a raw docstring for the linear-combination
  helper, which removes NumPy API drift warnings from the promoted M3 slice
- the normal-mode eigenproblem/eigensolver tests now work on the pixi path
  after:
  - switching old pytest =setup()= hooks to =setup_method()=
  - replacing positional Matplotlib =legend(..., 'lower center')= calls with
    the modern keyword form
  - adding a local DOLFIN 2019 timing fallback in
    =finmag.normal_modes.eigenmodes.eigensolvers=
- the field setter/field regression files now work on the pixi path after:
  - switching the large =TestField= fixture class to =setup_method()=
  - replacing removed PETSc vector =.array()= and =.abs()= assumptions in the
    tests with =get_local()= and NumPy-based comparisons
  - treating the removed DOLFIN Python =Function.label()= accessor as optional
- =finmag.sim.Simulation.schedule()= now uses a Python 3.11-safe
  =inspect.getfullargspec= fallback rather than the removed
  =inspect.getargspec=
- =finmag.util.meshes= now falls back to the Netgen Python API when the
  conda-forge package does not provide the historical =netgen= CLI, and
  launched-and-hung Netgen probes raise rather than silently turning into
  skips
- =finmag.util.meshes= now uses Matplotlib's supported
  =add_subplot(..., projection='3d')= path for 3D plotting on this stack
- =finmag.util.helpers= and its exercised callers now use =get_local()= on
  PETSc-backed vectors, accept =UserExpression=-compatible callable helpers,
  and avoid the removed =df.parameters.reorder_dofs_serial= attribute on the
  DOLFIN 2019 path
- =src/finmag/util/mesh_templates_test.py= now uses a looser combined-mesh
  volume tolerance on this path to account for the small numerical
  differences in regenerated Netgen geometry
- =src/finmag/util/length_scales_test.py= now uses a pytest-9-compatible
  =setup_method()= hook instead of the old bare =setup()=
- =src/finmag/util/vtk_saver_test.py= now initializes PETSc-backed vectors
  with =set_local()= instead of the removed =.array()= accessor
- =src/finmag/drivers.sundials_integrator.SundialsIntegrator.advance_steps()=
  now treats the expected "stop after mxstep internal steps" condition as
  success based on actual step-count deltas, which makes the wrapper robust to
  newer SUNDIALS bindings that surface the stop as a generic
  =RuntimeError: CVode has returned error -1=
- the Robertson stiff-ODE file now uses =scipy.integrate.solve_ivp= with
  =method="BDF"= as the SciPy-side success reference; direct investigation
  showed that old =ode(..., "vode")= /= BDF= completed correctly on
  SciPy =0.19.1= but fails badly on SciPy =1.17.1= for the same equations and
  Jacobian, while =solve_ivp(BDF)=, =solve_ivp(Radau)=, =solve_ivp(LSODA)=,
  and =zvode= still converge to the correct Robertson asymptotic state
- the two transposed-Robertson cases are now explicit =xfail(strict=True)=
  tests of known pathological failure modes on the current stack, rather than
  passing negative tests that only asserted work-budget exhaustion
- =src/finmag/tests/jacobean/test_native_llg.py= now passes on the DOLFIN
  2019 path by using =get_local()= instead of the removed PETSc
  =.array()= accessor when passing alpha values into the native LLG helper
- OOMMF-backed comparisons now run on the pixi path because =oommf= is a
  conda-forge dependency. The promoted DMDT and exchange comparison tests were
  adjusted to use =get_local()= for DOLFIN-2019 PETSc vectors instead of the
  removed legacy =.array()= accessor. [Codex GPT-5.4]
- the remaining Magpar comparison table labels now escape the literal
  backslash in =:math:`\,`=, avoiding Python invalid-escape warnings while
  preserving the generated reST text. [Codex gpt-5.5 high]
- =compute_normal_modes_generalised()= in the deprecated normal-mode module
  now uses a deterministic alternating ARPACK start vector when =v0= is not
  supplied, stabilising the Kittel-mode regression on SciPy =1.17.1=. [Codex
  gpt-5.5 high]

Still out of the current M3 gate:

- full NEB tangent computations still require =finmag.native.neb=, even
  though the helper-only regression file =src/finmag/physics/tests/neb/neb_test.py=
  now runs in M3
- =nsim= remains intentionally outside the pixi environment; the current M3
  track should continue to rely on checked-in Nmag reference data where that
  is sufficient, rather than trying to install Nmag. [Codex GPT-5.4]
- =treecode_bem= remains unavailable on the pixi native build, but the PBC
  demag regression file is now represented in M3 as a collected skip. The
  agreed follow-up is to build and activate =finmag.native.treecode_bem= under
  pixi, then unskip the PBC demag checks after native validation; a NumPy
  fallback is not the next step. [Codex gpt-5.5 high]

** First M4 / DOLFINx Dependency Probe

M4 now has an isolated pixi environment rather than sharing the FEniCS-2019
default environment:

- =pixi.toml= defines a =dolfinx= feature with =python = "3.12.*"=,
  =fenics-dolfinx=, =pytest=, and =numpy=
- the =dolfinx= environment uses =no-default-feature = true= so it does not
  inherit the default =fenics 2019.1.0= stack
- =dev/bin/verify-dolfinx-m4= runs =dolfinx-import= and =dolfinx-smoke=
- =dev/dolfinx/test_dolfinx_smoke.py= captures the same tiny DOLFINx
  mesh/function workflow as a pytest regression
- =.github/workflows/dolfinx-m4.yml= wires the same probe into CI

Verified local result:

- =dolfinx 0.10.0 rank 0=
- =dofs 9 sum 13.5=
- =2 passed= for =dev/dolfinx/test_dolfinx_smoke.py=

This is intentionally only a dependency and tiny mesh/function smoke probe. It
creates a clean place to start M4 without perturbing the green M2/M3 gates.
[Codex gpt-5.5 high]

The next small M4 step adds a DOLFINx-only prototype module:

- =dev/dolfinx/prototype.py= creates vector-valued magnetisation function
  spaces, interpolates constant vector fields, assembles exchange energy
  =A * integral(grad(m) : grad(m))=, assembles the constant-field Zeeman energy
  =-mu0 * Ms * integral(m . H)=, and assembles constant-axis uniaxial
  anisotropy energy =K * integral(1 - (m . axis)^2)=
- =dev/dolfinx/test_prototype.py= compares that assembled Zeeman energy with
  the analytical unit-square value, checks =unit_length= scaling, and covers
  analytical uniaxial anisotropy cases including axis normalisation and
  zero-axis validation. It also checks exchange energy for constant and linear
  fields, including the explicit gradient scaling convention
  =unit_length ** (dim - 2)=. [Codex gpt-5.5 high]
- the prototype includes a deliberately small explicit normalized LLG step for
  a constant effective field. This checks a first DOLFINx-backed
  time-integration workflow without claiming to be a production solver.
  [Codex gpt-5.5 high]
- =dev/dolfinx/relaxation_example.py= ties the prototype into a small
  end-to-end example: mesh creation, magnetisation setup, exchange/Zeeman/
  anisotropy energy evaluation, explicit LLG stepping, and JSON summary output.
  The wrapper task =dolfinx-example= writes
  =/tmp/finmag-dolfinx-relaxation-summary.json= by default. [Codex gpt-5.5
  high]
- the JSON summary has =schema_version = 1= and a hand-written validator,
  keeping the first M4 output contract explicit without adding a JSON-schema
  dependency. [Codex gpt-5.5 high]
- =dev/dolfinx/README.md= records the supported prototype scope and explicit
  non-scope. This is important because M4 is a reduced prototype, not a
  promise of legacy =finmag.Simulation= compatibility. [Codex gpt-5.5 high]
- the relaxation example uses small dataclasses for parameters and
  JSON-compatible results. Treat these as an API sketch for review, not as a
  stable public API. [Codex gpt-5.5 high]
- the reduced parameter dataclass now validates constants and vector sizes
  before DOLFINx form assembly. [Codex gpt-5.5 high]
- the relaxation example CLI path is covered by pytest, including the JSON
  printed to stdout and written to disk. [Codex gpt-5.5 high]
- invalid relaxation example CLI controls are covered by pytest, keeping the
  wrapper-facing path aligned with function-level validation. [Codex
  gpt-5.5 high]
- summary validation checks the nested =parameters= block, including missing
  keys and invalid vector values. [Codex gpt-5.5 high]
- the relaxation example is tested for deterministic JSON summaries across
  repeated runs with identical controls. [Codex gpt-5.5 high]
- vector-parameter validation rejects wrong sizes and non-numeric values before
  DOLFINx form assembly. [Codex gpt-5.5 high]
- =pixi.toml= runs all =dev/dolfinx= tests through =dolfinx-pytest=

This is deliberately not imported from =src/finmag= yet. The point is to pin
down DOLFINx form/function semantics before attempting a Finmag-compatible
public API. Local verification: =25 passed= for =pixi run -e dolfinx
dolfinx-pytest=. [Codex gpt-5.5 high]

M4 reduced-prototype status: the original completion criteria are represented
by the current =dev/dolfinx= lane. There is one end-to-end DOLFINx example,
documented supported scope, and explicit unsupported subsystems. Treat this as
a stable base for M5, not as a production port. [Codex gpt-5.5 high]

** DOLFINx Porting Strategy

The current =dev/dolfinx= work is an exploration lane. It should answer
practical DOLFINx questions and provide executable witnesses, but it should not
become a clean-room mini-Finmag or the default architecture for the final port.
[Codex gpt-5.5 high]

For the actual port from FEniCS 2019.1 to DOLFINx, preserve the existing Finmag
software design as far as practical: public API, =Simulation= orchestration,
energy modules, drivers, field/data abstractions, restart/output conventions,
and the existing test suite should remain the baseline. Any departures should
be justified by DOLFINx semantics, Python/MPI/runtime changes, packaging
constraints, or other concrete software-stack changes. [Codex gpt-5.5 high]

Use the green FEniCS-2019/Python-3 tests as the behavioural contract. If the
DOLFINx port needs different behaviour, change the tests explicitly and record
why. Do not let the exploratory =PrototypeSimulation= API become the production
API unless that choice is made deliberately after mapping it back to the
existing Finmag design. [Codex gpt-5.5 high]

=dev/dolfinx/porting_map.md= records the main legacy surfaces to preserve, the
current DOLFINx exploration evidence, and the readiness criteria for editing the
matching =src/finmag= module directly. Prototype implementations remain in the
exploration lane rather than being moved into production.

The FK demag baseline in the M3/pixi path still uses compiled native BEM code:
=finmag.native.llg.compute_bem_fk= or the DOLFIN-2019-compatible
=compute_bem_fk_from_arrays= entry point. The Python/NumPy Magpar code is a
small-reference/comparison path, not the production FK BEM implementation.
Keep PBC/treecode demag separate because it depends on the missing
=finmag.native.treecode_bem= extension. [Codex gpt-5.5 high]

=pixi run native-fk-bem-smoke= now gives that compiled FK BEM baseline its own
small witness by building a tiny boundary mesh and calling
=compute_bem_fk_from_arrays=. The M3 verifier runs this before the larger test
suite so a missing or broken native FK BEM backend is reported directly.
[Codex gpt-5.5 high]

=dev/bin/verify-python3-native-fk-bem-smoke= exposes the same check as a
single-purpose diagnostic command for CI triage. [Codex gpt-5.5 high]

** Revised Direct-Source DOLFINx Migration Decision

The production DOLFINx port will not be developed as a second =finmag= package
under =dev/dolfinx= and copied into place later. The existing modules directly
under =dev/dolfinx= remain bounded mechanics probes. Once a capability's
DOLFINx behavior and legacy contract are understood, the corresponding
=src/finmag= module is edited directly and its existing tests are ported in
place.

This avoids duplicate implementations, API drift, and a final delete/move diff.
After the first DOLFINx-only source edit, commit
=ba9280934e188d7f3800e7b9865e70a9422f7687= is the immutable
Python-3/FEniCS-2019 oracle and is run from a separate process or checkout when
new numerical reference data is required. Production source will not acquire a
general legacy-DOLFIN/DOLFINx compatibility facade merely to keep both stacks
live in one checkout.

Two focused probes explain why the current prototype must remain evidence only:

- a restart state with cubic-anisotropy values =(7, 3, 2)= restores them as
  =(0, 0, 0)= because the reader does not reconstruct the newer fields;
- a nonuniform nodal field averaged on two MPI ranks gives a different result
  from the serial calculation because ghost entries are included in the global
  sum and count.

The direct-port design and task sequence are recorded in
=docs/superpowers/specs/2026-07-06-dolfinx-core-port-design.md= and
=docs/superpowers/plans/2026-07-06-dolfinx-core-port.md=.

The migration control plane now distinguishes the frozen prototype witness
from future direct source gates:

- the DOLFINx environment pins =fenics-dolfinx = "0.10.*"= and includes
  SciPy;
- =dolfinx-prototype-pytest= is the canonical name for the unchanged M4 test
  scope, while =dolfinx-pytest= remains a compatibility alias;
- future production slices use =dolfinx-src-*= task names and are not added to
  the prototype verifier;
- =dolfinx-versions= emits a sorted JSON report for DOLFINx, Python, NumPy,
  SciPy, PETSc, and MPI;
- the M2, M3, and frozen DOLFINx verifiers snapshot the tracked diff and fail
  if validation changes it;
- M2/M3 read-only validation exports =WRITE_FINMAG_VERSION=0= so a native
  rebuild cannot rewrite =src/finmag/__version__.py=. Normal native builds
  retain the historical version-generation default. [Codex GPT-5]

Verified on 2026-07-20:

- DOLFINx =0.10.0=, Python =3.12.13=, NumPy =2.4.6=, SciPy =1.18.0=,
  PETSc/petsc4py =3.25.2=, mpi4py =4.1.2=, MPI standard =3.1=, and Open MPI
  =5.0.10=;
- the frozen prototype suite remains =105 passed in 2.47s=;
- import, mesh/function smoke, relaxation JSON, and restart round-trip checks
  passed;
- the M2 workflow passed after a real native rebuild, and its tracked-diff
  guard confirmed that =src/finmag/__version__.py= was not rewritten. [Codex
  GPT-5]

** Immutable FEniCS-2019 Oracle Contract

The immutable Python-3/FEniCS-2019 oracle is commit
=ba9280934e188d7f3800e7b9865e70a9422f7687=. Its exact aggregate M2 command is:

#+begin_src bash
dev/bin/verify-python3-m2
#+end_src

At that commit the wrapper expands, in order, to:

#+begin_src bash
pixi run import-finmag
pixi run barmini-construct
pixi run barmini-smoke
pixi run restart-smoke
#+end_src

The contemporaneous record says all four succeeded against FEniCS =2019.1.0=:
plain import, full default =barmini= construction, advancement to =1e-12= on
native Sundials =7.2.1= with FK demag, and save/restart/reload. No aggregate M2
test count or runtime was recorded, so none should be inferred. A current
pre-production-source rerun on 2026-07-20 also passed all four after a native
rebuild; that current control-plane result is evidence about the runner, not a
replacement numeric result for the historical oracle.

The exact aggregate M3 command at the oracle is:

#+begin_src bash
dev/bin/verify-python3-m3
#+end_src

It expands to:

#+begin_src bash
pixi run native-fk-bem-smoke
pixi run barmini-suite
#+end_src

The recorded oracle result is =493 passed, 21 skipped, 5 xfailed= in
approximately =730s=. This was not rerun during the control-plane work because
the existing result is tied to the immutable commit and lock, and the full gate
takes about twelve minutes.

=dev/bin/run-legacy-oracle= now runs focused argv commands in a disposable
detached worktree at that exact commit. A locked import probe completed and
reported Finmag =ba9280934e188d7f3800e7b9865e70a9422f7687=, DOLFIN =2019.1.0=,
Python =3.11.15=, NumPy =1.26.4=, SciPy =1.17.1=, and Sundials =7.2.1=. The
helper removed the worktree after both successful and deliberately failing
command probes without changing the active tracked state. Fixture shape and
analytic-versus-oracle rules are recorded in
=docs/superpowers/specs/legacy-oracle-fixtures.md=. [Codex GPT-5]

Native-code caveat: =finmag.native.llg.compute_bem_fk_from_arrays= accepts the
right array-level inputs for a DOLFINx demag port, but the current extension as
a whole still links =libdolfin=, compiles =swig_dolfin= converters, and
registers those converters at module import. The array-only LLG/BEM binding must
therefore be separated from the legacy DOLFIN mesh surface before rebuilding it
on the Python-3.12/DOLFINx stack.

** Lazy Source Import Boundary

The first direct source slice replaces wildcard package initialization with
explicit PEP 562 lazy exports in =src/finmag/__init__.py= and
=src/finmag/energies/__init__.py=. The reviewed public-name inventories are
recorded in the direct-port design. Plain package discovery now imports neither
=dolfin=, =dolfinx=, nor =finmag.native=; requesting an unported legacy object
in the DOLFINx environment fails at the real missing =dolfin= dependency.

Importing DOLFINx =0.10.0= separately showed that it does not load legacy
=dolfin=, does not load Finmag, and exposes no =dolfinx.parameters= equivalent
to the old =reorder_dofs_serial= initializer setting. No DOLFINx initializer
side effect is therefore required. Legacy public-object resolution still sets
=dolfin.parameters["reorder_dofs_serial"] = False= before loading the object.
Because direct =from finmag.field import Field= bypasses the root resolver,
=field.py= makes the same small compatibility call after importing =dolfin=;
this is relocation of the old initializer contract, not a Field port.

Verified on 2026-07-20:

- =pixi run -e dolfinx dolfinx-src-import= passed;
- =pixi run -e dolfinx dolfinx-src-import-pytest= reported =4 passed, 2
  skipped= in =1.60s=;
- =pixi run src-import-pytest= reported =4 passed, 2 skipped= in =7.02s=;
- the same active-source test run through =dev/bin/run-legacy-oracle= with the
  oracle's locked environment reported =4 passed, 2 skipped= in =12.79s=;
- an oracle inventory command confirmed the defining modules for all retained
  root and energy exports before the lazy boundary was implemented;
- =dev/bin/verify-python3-m3= completed with exit status zero against the
  active lazy-import source after the native FK BEM smoke passed. The terminal
  transport did not retain pytest's final count line, so the historical
  =493 passed, 21 skipped, 5 xfailed= count is not restated as a newly measured
  result. [Codex GPT-5.6]

** Field Ownership And Ordering Probe

Task 4's pre-source-edit probe established that DOLFINx =Function.x.array=
contains owned blocks followed by ghost blocks, unlike the owned-only legacy
=GenericVector.get_local()= result used by =Field.as_array()=. On a
=UnitSquareMesh(3, 3)= with 16 global CG1 vertices, the DOLFINx ranks own 7/9
blocks and store 5/3 additional ghosts; block size three produces owned raw
lengths 21/27 and full local lengths 36/36. The equivalent legacy run owns
6/10 scalar dofs (18/30 vector scalar entries) while each rank's mesh exposes
10 vertices including ghosts.

The accepted compatibility rules before editing production =Field= are:

- raw arrays are flat rank-local owned backend-order values; setting them is
  followed by =scatter_forward()=;
- =xyz= is flat coordinate-ordered per-node data and =xxx= is its explicit
  component-blocked transpose, not a native DOLFINx layout;
- rank-local coordinate/value and ordered-array exports exclude ghosts;
- =Field.average()= remains a global volume-weighted FEM integral;
- the prototype-only nodal summary averages owned nodal blocks once;
- normalization modifies owned nodes, detects zeros collectively, and then
  refreshes ghosts.

The old ghost-inclusive nodal reduction returned
=[1.38888889, 2.37037037, 4]= for the nonuniform two-rank witness instead of
=[1.38888889, 2.38888889, 4]=. The bounded helper is corrected and the new
=dev/dolfinx/field_ownership_probe.py= checks the fixed result, raw ownership,
normalization, flat =xyz=/=xxx= round trips, and a gathered union of 16 unique
analytic coordinate/value rows.

Verified on 2026-07-20:

- =pixi run -e dolfinx dolfinx-field-ownership-probe= passed with 16 owned
  blocks and no ghosts on one rank;
- =pixi run -e dolfinx dolfinx-field-ownership-probe-mpi= passed with 7/9
  owned and 5/3 ghost blocks;
- the focused DOLFINx adapter tests reported =31 passed in 1.51s=;
- the frozen prototype gate remained =105 passed in 2.23s=;
- the unchanged legacy =field_test.py= and =field_setters_test.py= reported
  =48 passed, 1 skipped in 126.95s=. [Codex GPT-5.6]

** Direct DOLFINx Field Port

Task 4 now edits =src/finmag/field.py= directly; no second package was built in
=dev/= and no prototype module was copied into place. The active =Field=
stores DOLFINx =fem.Function= data and makes the migration contract explicit:

- =as_array()= and =from_array()= use flat rank-local owned backend-order
  values, and all mutations call =scatter_forward()=;
- coordinate/value rows are owned rank-local exports; scalar values have shape
  =(n_owned,)= and vector values =(n_owned, components)=, while =xyz= and
  =xxx= are flat arrays with =xxx= implemented as the deliberate
  component-blocked compatibility transpose;
- constants, DOLFINx =Constant=/=Function= objects, Fields, naturally
  vectorized callables, and legacy point-at-a-time Python callables are
  accepted; cross-space Field assignment interpolates;
- =average(dx=...)= assembles a global FEM volume average using the passed
  measure, including restricted/subdomain measures;
- normalization detects a zero on any rank collectively, updates owned nodal
  blocks, and refreshes ghosts;
- VTK/PVD and XDMF output use DOLFINx-native writers.

All reductions and ghost-refreshing mutations are documented as collective.
Scalar detection uses the UFL reference value shape, preserving the distinction
between a scalar element and a blocked one-component vector. Zero-volume
rejection is exact rather than tolerance-based so physically valid nanometre-
scale measures are not rejected.

The independent pre-commit review found and resolved four gaps that the first
unit-square gate did not expose:

- a tolerance-based zero-volume check rejected valid domains smaller than
  =1e-8=; an exact-zero check and a =1e-9=-length regression now preserve the
  scale-independent average;
- a blocked one-component vector was mistaken for a scalar when inspecting the
  collapsed DOLFINx storage shape; UFL's reference value shape now distinguishes
  the two, with normalization and vector averaging tested;
- =allclose()= could compare unrelated same-shaped local buffers; it is now a
  documented collective requiring congruent communicators and the same mesh
  and function-space objects, with both equal and unequal two-rank cases;
- the production parity matrix now covers mesh dimensions 1/2/3, scalar and
  1/2/3/4-component fields, underlying mesh/dofmap/vector/PETSc accessors, and
  normalized ghost values on two ranks. No medium-or-higher finding remained
  after re-review.

The old =field_test.py= and =field_setters_test.py= remain unchanged as oracle
history. Focused production tests live in
=src/finmag/tests/test_field_dolfinx.py=, and the two-rank executable gate is
=src/finmag/tests/field_dolfinx_mpi_probe.py=. Expression strings,
=Expression=/=UserExpression=, point probing and point-measure arithmetic,
legacy plotting, spherical conversion, and =dolfinh5tools= HDF5 now raise
precise =NotImplementedError=s rather than failing incidentally.

The root =finmag.Field= export now resolves without loading legacy =dolfin=.
Other public objects such as =Simulation= still cross legacy modules and are
intentionally deferred; the lazy import test checks their real missing
dependency in the DOLFINx-only environment. In the legacy-only pixi lane, the
old downstream public-name resolution check is skipped after this DOLFINx-only
module cutover, while the package's safe lazy import boundary remains tested.

Verified on 2026-07-20:

- =pixi run -e dolfinx dolfinx-src-field-pytest= reported =26 passed in
  1.63s=, including mesh dimensions 1/2/3, scalar and 1/2/3/4-component
  fields, and a =1e-9=-length measure;
- =pixi run -e dolfinx dolfinx-src-field-mpi= passed with 7/9 owned and 5/3
  ghost blocks and 16 gathered global vertices, including collective
  =allclose()= and normalized-ghost checks;
- =pixi run -e dolfinx dolfinx-src-import-pytest= reported =4 passed, 2
  skipped in 1.70s=;
- =pixi run src-import-pytest= reported =3 passed, 3 skipped in 0.91s=;
- =dev/bin/verify-dolfinx-m4= remained green, including =105 passed in
  1.94s=. [Codex GPT-5.6]

** Task 5 Energy Foundation Probe

The Task 5 source audit found that the energy package was syntactically lazy
but not backend-sliceable: =finmag.energies.__getattr__= called the legacy
=dolfin= initializer for every exported interaction. The direct port now lets
=EnergyBase=, =Exchange=, static =Zeeman=, and =UniaxialAnisotropy= bypass that
bridge, while untouched interactions retain the real missing-=dolfin= failure.
The ported modules also removed imports of =finmag.util.meshes=,
=finmag.util.helpers=, and =finmag.native= so those dependencies cannot reopen
the legacy backend transitively.

The legacy default =box-matrix-petsc= method could not remain the default when
the first supported DOLFINx algorithm is =box-assemble=. Task 5 changes the
default to the usable box-assembly path and rejects historical matrix, project,
and direct requests explicitly. Mapping those names to assembly would conceal
an algorithm change. Existing legacy Exchange, Zeeman, and anisotropy test
files remain oracle history because they import =dolfin= at collection time;
their analytical cases were translated into a compact DOLFINx production
matrix rather than edited into a second backend package.

=dev/dolfinx/energy_box_probe.py= now freezes the DOLFINx 0.10 ownership
sequence before production edits. Linear-form vectors first
=scatter_reverse(InsertMode.add)= to accumulate contributions into the owning
rank and then =scatter_forward()= to refresh completed ghost copies. Only after
that sequence may owned coefficients be divided by lumped nodal volumes. The
probe checks:

- scalar and three-component nodal volumes summing globally to the unit-square
  area without double-counting ghosts;
- Zeeman =H_eff == H=, including the negative variational sign, =mu0=, and
  cancellation of =Ms=;
- the constant-axis =K1= and =K2= anisotropy energy and field formulas;
- analytical linear-field exchange energy and the =unit_length**-2= effective
  field scaling;
- complete owner/ghost values in serial and on two ranks.

Verified on 2026-07-20:

- the serial probe passed with 16 owned blocks and no ghosts;
- the two-rank probe passed with 7/9 owned blocks and 5/3 ghosts, and both runs
  recovered global volume =1.0=. [Codex GPT-5.6]

** Direct DOLFINx Common-Energy Port

Task 5 edits the existing modules under =src/finmag/energies= directly. No
energy package was recreated in =dev/= and the validated probe was not copied
into production. The accepted production contract is:

- setup and every FEM assembly/reduction operation are collective over the
  magnetisation mesh communicator;
- the supported magnetisation space is explicitly a blocked three-component
  CG1 space; higher-order/mixed layouts are rejected before lumped weights can
  fail inconsistently across ranks;
- assembled linear forms reverse-add into owners and forward-refresh ghosts,
  while public =compute_field()= arrays contain flat owned DOLFINx backend
  order only;
- total energy performs one scalar MPI sum and applies
  =unit_length**topological_dimension=; exchange alone puts the additional
  =unit_length**-2= factor in its gradient density;
- =EnergyBase= provides box-assembled field, total energy, lumped nodal energy
  density, refreshed density Function, and owned-only global nodal average;
- static =Zeeman= accepts constants/callables, keeps its =H= Function live
  across =set_value()=, rebuilds it when =setup()= moves to a new mesh,
  preserves restricted =dx= energy assembly and legacy pointwise nodal energy
  density, and returns =H_eff == H=;
- =Exchange= accepts a finite scalar =A= and retains the public DG0 =A= Field;
- uniaxial anisotropy preserves the repository's exact
  =K1*(1-s**2)-K2*s**4= law and dynamically reassembles the nonlinear =K2=
  field after =m= changes;
- Exchange and anisotropy retain their validated constructor inputs so a
  deliberate second =setup()= can rebuild coefficient Fields/forms on a new
  mesh instead of failing through deleted state; cached density Functions are
  invalidated so they cannot remain attached to the old mesh.

The anisotropy source had lost axis normalization during the 2014 Field
migration even though the API describes the axis as a unit vector and commit
=eb504a0a= introduced normalization so =axis dot m= is a cosine. The DOLFINx
port restores that intended behavior: non-zero constant axes are normalized
and zero axes are rejected. This is a deliberate correction of a historical
regression, not an accidental numerical change.

The bounded slice requires positive constant =Ms= and finite positive
=unit_length=. Spatially varying =A=, =K1=, =K2=, anisotropy axes, and =Ms=
raise precise =NotImplementedError=s. =DipolarField= and the time,
discrete-time, Python-time, and oscillating Zeeman variants are explicit
deferred stubs. Matrix, project, native/direct anisotropy, PBC/region
interaction behavior beyond static Zeeman's passed measure, and downstream
=EffectiveField=/=Simulation=/hysteresis/LLB/normal-mode integration remain
future tasks.

Independent review found and resolved five gaps beyond the first analytical
matrix:

- the first lazy-map draft left the new time-Zeeman stubs behind the legacy
  bridge, producing a false missing-=dolfin= error; they now resolve without
  =dolfin= and fail on construction with the intended deferral;
- arbitrary higher-order spaces could reach non-positive row-sum lumped
  weights; the supported blocked three-component CG1 boundary and collective
  invalid-volume check are now explicit;
- repeated static-Zeeman setup reused an =H= Function from the old mesh and
  its first density implementation used a lumped projection rather than the
  legacy pointwise nodal product; both lifecycle and density contracts now
  have nonuniform regressions;
- callable/string spatial coefficients failed incidentally, and Exchange/
  anisotropy deleted the values needed to rebind to a new mesh; every deferred
  input now raises the documented error and repeated setup is tested;
- =EnergyBase= kept a cached density Function across re-setup; setup now
  invalidates it, and the mesh-rebind regression checks the replacement.

Verified on 2026-07-20:

- =pixi run -e dolfinx dolfinx-src-energies-pytest= reported =26 passed=;
- =pixi run -e dolfinx dolfinx-src-energies-mpi= passed with 7/9 owned and 5/3
  ghost blocks, including owner/ghost field agreement, global volume, energy
  density reduction, static/callable Zeeman averages, Exchange scaling, and
  =K1=/=K2= anisotropy;
- =pixi run -e dolfinx dolfinx-src-import-pytest= reported =6 passed, 2
  skipped=;
- =pixi run src-import-pytest= reported =3 passed, 5 skipped=. [Codex GPT-5.6]
- the unchanged immutable-oracle Exchange, anisotropy, and Zeeman suites
  reported =28 passed in 185.89s=; the oracle worktree integrity guard also
  completed successfully. [Codex GPT-5.6]

** Direct DOLFINx EffectiveField Port

Task 6 ports =src/finmag/physics/effective_field.py= directly. The registry
API is unchanged: =add=/=get=/=exists=/=all=/=remove=, the unique-name
=ValueError= on duplicate =add=, =UnknownInteraction= on missing =get=/
=remove=, =compute=/=compute_jacobian_only=/=total_energy=, and the
=with_time_update= callback contract including the "some interactions
require a time update, but no time step was given" error when =t= is omitted.

Three legacy surfaces were replaced:

- =self.m_field.f.vector().local_size()= (legacy dolfin) is now
  =self.m_field.as_array().size=, i.e. the DOLFINx =Field='s flat rank-local
  owned-dof count -- the same owned-array contract every ported interaction's
  =compute_field()= already returns, so =H_eff= accumulation stays
  shape-consistent.
- =get_dolfin_function= no longer imports
  =finmag.util.helpers.vector_valued_function= (which pulls in legacy
  =dolfin= at module import time). It is reimplemented directly using the
  ported =Field=: =Field(self.m_field.functionspace,
  interaction.compute_field()).f= builds a fresh DOLFINx =Function= on m's
  space from the interaction's owned field array, matching the legacy
  contract of returning a bare Function (mirroring how
  =Simulation.llg._m_field.f= already returns one for ~m~ itself). The
  historical =region= parameter was already unused by the old
  implementation's call to =vector_valued_function=; rather than preserve
  that silent no-op, the DOLFINx port raises an explicit
  =NotImplementedError= naming region-restriction as deferred instead of
  quietly ignoring the argument.
- =from finmag.energies import TimeZeeman= is preserved unchanged. Task 5
  already made =TimeZeeman= resolve to an explicit deferred stub
  (=_DeferredZeeman= subclass) without requiring legacy =dolfin=, so this
  import and the =isinstance(interaction, TimeZeeman)= auto-connection check
  both work exactly as before; only actually constructing a =TimeZeeman=
  still raises its documented deferral. The new test suite exercises the
  auto-connection with a plain-Python subclass double that overrides
  =__init__= (so =isinstance= still succeeds without triggering the raise),
  confirming =with_time_update= is wired to =interaction.update= exactly as
  the legacy behavior intended.

=src/finmag/physics/__init__.py= is empty, so importing
=finmag.physics.effective_field= directly never pulled in unrelated legacy
subsystems; no import-boundary change was needed there.

New focused tests (=src/finmag/tests/test_effective_field_dolfinx.py=) cover:
registry behavior against plain-Python interaction doubles independent of
FEM assembly (add/get/exists/all/remove, duplicate-name rejection,
=UnknownInteraction=); total field and total energy summed across real
=Exchange=, =Zeeman=, and =UniaxialAnisotropy= instances on a small
DOLFINx mesh; the legacy compute()-returns-a-copy regression (mutating a
returned array must not corrupt the next =compute()= result); the
time-update callback contract including the "no t given" =ValueError=; the
=TimeZeeman= auto-connection double described above; and that removing an
interaction changes the accumulated field array itself (not only the
reported energy), verified against the removed interaction's own
=compute_energy()= delta. A two-rank probe
(=src/finmag/tests/effective_field_dolfinx_mpi_probe.py=) checks the same
total-field/total-energy consistency, the copy contract, and the
remove-changes-field behavior collectively, with total energy independently
confirmed equal across both ranks.

=Simulation=, hysteresis, LLB, and normal-mode consumers of
=EffectiveField= remain unported.

Verified on 2026-07-21:

- =pixi run -e dolfinx dolfinx-src-effectivefield-pytest= reported =15
  passed=;
- =pixi run -e dolfinx dolfinx-src-effectivefield-mpi= passed with 7/9 owned
  and 5/3 ghost blocks;
- =pixi run -e dolfinx dolfinx-src-import-pytest= reported =6 passed, 2
  skipped= (unchanged);
- =pixi run -e dolfinx dolfinx-src-field-pytest= reported =26 passed=
  (unchanged);
- =pixi run -e dolfinx dolfinx-src-energies-pytest= reported =26 passed=
  (unchanged);
- =dev/bin/verify-dolfinx-m4= remained green, including =105 passed= for
  =dolfinx-prototype-pytest=.

The new =dolfinx-src-effectivefield-pytest=/=dolfinx-src-effectivefield-mpi=
pixi tasks were deliberately *not* added to =dev/bin/verify-dolfinx-m4=: that
script is the frozen M4 prototype gate, and the existing convention (recorded
above under "Lazy Source Import Boundary" and followed by every
=dolfinx-src-*= task since) is that direct production-port gates stay
separate, focused tasks rather than being folded into the frozen aggregate
verifier. [Claude Sonnet 5]

** Direct DOLFINx LLG Port

Task 7 ports =src/finmag/physics/llg.py= directly to DOLFINx. The deterministic
core (=LLG=, scalar =Ms=, scalar =alpha=, =gamma=, =set_m=, =solve=,
=solve_for=, =pins=) is now pure NumPy on top of the ported =Field= and
=EffectiveField=; no compiled =finmag.native.llg= extension, =dolfin=,
=finmag.util.helpers=, or =finmag.util.meshes= import remains. Importing
=finmag.physics.llg= in the DOLFINx env pulls in only =numpy=,
=dolfinx.fem= (for the DG0 =Ms= space), =finmag.util.consts= (pure
constants), =finmag.field=, and =finmag.physics.effective_field=.

The exact node-local right-hand side was transcribed from the legacy native
kernel =calc_llg_dmdt= in =native/src/llg/llg.cc= (=damping_i= line 35,
=relaxation_i= line 105, =precession_i= line 74, assembled in the per-node loop
at lines 194-200). With =gamma_LL = gamma / (1 + alpha^2)= the update is

  dm/dt = -gamma_LL (m x H)
          - alpha gamma_LL (m x (m x H))
          + c (1 - |m|^2) m

The final term is the legacy numerical norm-relaxation correction: the native
code passes =0.1/char_time= as =relaxation_i='s coefficient, and =solve= sets
=char_time = 0.1/self.c=, so the coefficient is exactly =self.c = 1e11 1/s=. It
vanishes for unit-length m and holds |m| = 1 during integration. =precession_i=
is gated by =do_precession= exactly as in the native loop; the damping and
relaxation terms are always applied. Pinning zeroes dm/dt at the listed node
indices, matching the native =pin(...)= call.

Ordering is made unambiguous, resolving the raw/=xxx= ambiguity flagged in the
porting map: =solve=/=solve_for= and the =m= setters use the component-blocked,
coordinate-ordered =xxx= state vector, and =H_eff= (accumulated by the registry
in raw owned-dof order) is routed through a throwaway =Field.from_array= then
=get_ordered_numpy_array_xxx()= so m and H share one node ordering before the
node-local cross/dot products. Every RHS evaluation goes through
=self.effective_field.update(t)= and the accumulated =H_eff=; there is no bypass
path.

Constructor shape is preserved: =LLG(S1, S3, do_precession=True, average=False,
unit_length=1)=, where =S1=/=S3= are now DOLFINx scalar/3-component CG1
=fem.functionspace= objects instead of legacy =df.FunctionSpace=/
=df.VectorFunctionSpace=. =average= is still accepted-and-ignored exactly as in
the legacy constructor. =Ms= remains a property returning a scalar DG0 =Field=;
its setter interpolates the constant and refreshes the cached =_Ms=/=Ms_av=.

Deliberate deferrals raise =NotImplementedError= by name rather than silently
degrading: =use_slonczewski= and =use_zhangli= (spin-transfer torque),
=sundials_jtimes=/=sundials_psetup=/=sundials_psolve= (native CVODE
preconditioner/Jacobian paths), and =set_alpha= with a non-scalar value
(spatially varying damping). =sundials_rhs= is kept as a backend-neutral
=ydot[:] = solve_for(y, t)= adapter for the coming SciPy driver slice. Multi-rank
ODE state is out of scope: the =xxx= state vector is rank-local owned-only, so
=solve=/=solve_for= and the =sundials_m= setter raise a serial-only guard when
=comm.size > 1= (construction and non-state methods still work on multiple
ranks). Thermal/SLLG dynamics live in separate untouched modules.

Reference strategy follows the fixture spec: the macrospin case uses the
closed-form Landau-Lifshitz right-hand side (no fixture -- analytic result fully
fixes sign, scale, and units), while the nonuniform case uses a coordinate-
ordered legacy oracle fixture. The fixture
(=src/finmag/tests/fixtures/llg_rhs_nonuniform.json=, generated by
=gen_llg_rhs_nonuniform.py= run at the immutable oracle via
=dev/bin/run-legacy-oracle=) is built on a 1D =IntervalMesh(4, 0, 4)=: a
1D mesh has identical edge connectivity in =dolfin= and =dolfinx=, so the box-
method Exchange field is isolated from tetrahedralisation-diagonal differences
that a 3D box mesh would introduce. It carries a spatially varying 3-component
m with Exchange + Zeeman and stores coordinate-sorted m, H_eff and dm/dt.

New focused tests (=src/finmag/tests/test_llg_dolfinx.py=, 19 cases) cover: the
import boundary; the closed-form macrospin RHS; precession sign (negative
gyromagnetic convention, m x H carries +x toward +y about +z); damping rotating
m toward H; the precession rate equalling =gamma_LL * H= (physical time scale);
=do_precession=False= removing only the precession component; unit-length
invariance plus full-period precession return under RK4 integration of
=solve_for=; registry-driven RHS (adding/removing an interaction changes dm/dt);
pinning zeroing dm/dt with out-of-range rejection; the =xxx= =solve_for= round
trip; every deferred surface raising by name; the serial-only multi-rank guard
(via a mocked =comm=); and the nonuniform oracle fixture within its declared
tolerances.

Verified on 2026-07-21:

- =pixi run -e dolfinx dolfinx-src-llg-pytest= reported =19 passed=;
- regressions unchanged: =dolfinx-src-import-pytest= =6 passed, 2 skipped=,
  =dolfinx-src-field-pytest= =26 passed=, =dolfinx-src-energies-pytest= =26
  passed=, =dolfinx-src-effectivefield-pytest= =15 passed=,
  =dolfinx-prototype-pytest= =105 passed=.

The new =dolfinx-src-llg-pytest= pixi task follows the established convention of
keeping direct production-port gates as separate focused tasks rather than
folding them into the frozen M4 verifier. No MPI gate is added for this slice;
the serial-only guard is covered by a focused serial test instead.
=Simulation=, the SciPy/Sundials drivers, STT, thermal, and demag consumers
remain unported. [Claude Opus 4.8]

- Deliberate =set_pins= deviation: legacy =set_pins= logged
  =logger.error(...)= for out-of-range pin indices and silently kept the
  previous =_pins= array unchanged; the ported =set_pins= instead raises
  =ValueError= for the same condition. This is a fail-fast improvement --
  silently keeping stale pins on a bad index is a latent correctness trap --
  and is covered by =test_out_of_range_pins_raise=. [Claude Sonnet 5]

** Direct DOLFINx SciPy Driver

Task 8 ports =src/finmag/drivers/scipy_integrator.py= and updates
=src/finmag/drivers/llg_integrator.py= to reuse SciPy's =scipy.integrate.ode=
(VODE, methods =bdf=/=adams=) directly against the Task 7 deterministic
=LLG=. SciPy (=1.18.0=) was already pinned in the =dolfinx= pixi environment by
Task 1; this slice only verifies it (=pixi run -e dolfinx python -c "import
scipy; print(scipy.__version__)"= reports =1.18.0=) rather than re-adding it.

The recorded raw/=xxx= ambiguity is resolved, not preserved: the porting map's
"Field =xxx= consumers" note flagged that the legacy driver seeded the ODE
state from =m_field.as_array()= (raw backend dof order) while =rhs= passed
that same array straight into =llg.solve_for=, which has always interpreted
its argument as the component-blocked =xxx= state vector. On DOLFINx's
interleaved blocked dof layout the two orderings are genuinely different
(not just occasionally coincident, as on some legacy meshes), so this port
routes both directions -- the initial seed and every write-back after
=advance_time= -- through =Field.get_ordered_numpy_array_xxx()= /
=set_with_ordered_numpy_array_xxx()=. A dedicated regression test
(=test_scipy_integrator_seeds_ode_state_with_xxx_not_raw_array=) asserts the
ODE's initial state equals the =xxx= array and differs from the raw array on
a multi-vertex fixture mesh where the two are provably different; a second
test (=test_feeding_raw_order_into_solve_for_gives_wrong_physics=) shows that
feeding the raw array into =solve_for= as if it were =xxx= changes the
computed dm/dt, i.e. reintroducing the legacy conflation would silently
change the physics.

=advance_time='s public interface and tolerance keywords are unchanged:
=ScipyIntegrator(llg, m0, reltol=1e-6, abstol=1e-6, nsteps=10000,
method="bdf", tablewriter=None, **kwargs)=, the =t == 0= first-call no-op
(matching Sundials, since re-integrating to =t=0= from =t=0= is rejected by
the underlying solver), and =n_rhs_evals= all behave exactly as before.
Two behavioural gaps are closed rather than carried forward:

- =advance_time(t)= with =t < self.cur_t= now raises =ValueError= by name
  before touching =scipy.integrate.ode=, instead of only surfacing as a
  =vode= "Illegal input" warning followed by a bare, optimisable-away
  =assert=.
- an unsuccessful =ode.integrate(t)= call now raises =RuntimeError= (naming
  the method and target time) instead of =assert self.ode.successful()=,
  which =python -O= would silently compile out.

=reinit()= previously only logged "This integrator doesn't support
reinitialisation." It now does real work, mirroring what the Sundials reinit
contract exists to serve (see =sundials_reinit_test.py=): it rebuilds the
underlying =scipy.integrate.ode= object and reseeds it from
=self.m_field.get_ordered_numpy_array_xxx()= at the current =cur_t=
(preserved unchanged), so an external modification to the field (e.g. a
changed applied field between =reinit()= calls) actually takes effect on the
next =advance_time=. Without =reinit()=, =advance_time= only ever reads from
=scipy='s own internal solver state, so an external field mutation is
silently overwritten by the stale continuation on the very next step; a
paired test demonstrates both sides of this (with vs. without =reinit()=,
=test_reinit_makes_external_field_modification_take_effect=). =n_rhs_evals=
is deliberately left as a lifetime total across =reinit()= calls (its
docstring never claimed "since last reinit"), unlike the native Sundials
counter, which is an implementation detail of the compiled CVode object, not
part of the documented public contract.

A small stiff-ODE probe (=test_vode_bdf_stiff_probe_matches_analytic_solution=)
confirms the installed =vode=/=bdf= path directly against
=scipy.integrate.ode=, independent of =LLG=: for =y' = lam(y - cos t) - sin
t= with =lam = -1000=, the closed-form solution =y(t) = cos(t) + (y0 - 1)
exp(lam t)= is matched to =1e-6= at =t=1=, confirming the classic
Sundials-era stiff-solver path still behaves correctly under the modern
pinned SciPy.

Backend selection (checklist item 6): =llg_integrator(llg, m0,
backend="scipy", **kwargs)= keeps exactly the same public shape and
=backend= argument (no new selection mechanism); only the *default value*
changes, from =backend="sundials"= to =backend="scipy"=. The native Sundials
extension remains unported to the modern conda-forge/DOLFINx stack, so an
explicit =backend="sundials"= request keeps raising =ImportError= by name
(unchanged pre-existing code path), while callers who omit =backend=
entirely now get the working =ScipyIntegrator= instead of an import failure.
This only changes behaviour for callers that relied on the bare default:
every automated legacy oracle test and =finmag.sim.sim.Simulation= /
=sim_with= already pass =backend= explicitly (=integrator_backend="sundials"=
by their own separate default), so none of them are affected; only a couple
of unexercised example/=__main__= scripts (e.g.
=compute_domain_wall_cobalt()= in =domain_wall_cobalt.py=) rely on the bare
factory default, and are out of scope for this slice's gates.

New focused tests (=src/finmag/tests/test_scipy_driver_dolfinx.py=, 19 cases)
cover: the import boundary; the stiff VODE/BDF probe; constructor defaults
and the =t=0= no-op; =cur_t=/=m_field= updates and =n_rhs_evals= growth after
=advance_time=; macrospin relaxation towards an applied field within a
damping-time-scaled physical duration; the resolved raw/=xxx= ordering
contract (three tests, including the "would fail under raw ordering"
regression above); backward-time and unsuccessful-integration rejection;
=reinit()= taking effect (and preserving =cur_t=); and the
=llg_integrator= default/explicit/=sundials=/unknown-backend behaviour.

Verified on 2026-07-21:

- =pixi run -e dolfinx python -c "import scipy; print(scipy.__version__)"=
  reported =1.18.0=;
- =pixi run -e dolfinx dolfinx-src-scipy-pytest= reported =19 passed= with
  pristine output (only the pre-existing =finmag/util/plot.py=
  =SyntaxWarning=s appear; the expected =vode= "Excess work done" warning
  from the unsuccessful-integration test is asserted with
  =pytest.warns(...)= rather than left as loose noise);
- regressions unchanged: =dolfinx-src-import-pytest= =6 passed, 2 skipped=,
  =dolfinx-src-field-pytest= =26 passed=, =dolfinx-src-energies-pytest= =26
  passed=, =dolfinx-src-effectivefield-pytest= =15 passed=,
  =dolfinx-src-llg-pytest= =19 passed=, =dolfinx-prototype-pytest= =105
  passed=.

The new =dolfinx-src-scipy-pytest= pixi task follows the established
convention of a separate focused production-port gate. No MPI gate is added;
the serial-only guard is inherited unchanged from =LLG.solve=/=solve_for=.
=Simulation= and the native Sundials driver remain unported; =solve_ivp= is
deliberately not introduced (out of scope per the plan). [Claude Sonnet 5]

** First M5 / DOLFINx Core Simulation Slice

M5 starts with a reduced core-path wrapper rather than a legacy API port:

- =dev/dolfinx/field_adapter.py= introduces a narrow DOLFINx-backed
  compatibility probe for the first legacy =finmag.field.Field= behaviours:
  scalar/vector inspection, constants, callables, nodal values, normalisation,
  and volume averages
- =dev/dolfinx/simulation.py= introduces =PrototypeSimulation=, composing the
  checked M4 DOLFINx helpers for mesh creation, magnetisation setup,
  exchange/Zeeman/anisotropy energy reporting, average magnetisation, one-step
  explicit LLG updates, and short relaxation histories
- =dev/dolfinx/test_simulation.py= covers supported energy terms, relaxation
  energy decrease, custom parameters, and invalid wrapper controls
- the wrapper exposes a JSON-compatible state summary that reports mesh label,
  average magnetisation, parameters, and energy terms without advancing the
  simulation
- the reduced wrapper exposes a JSON-compatible relaxation summary as a basic
  output path for the first M5 core sketch
- the wrapper summary is checked with the existing relaxation summary validator
  and includes =dolfinx_version= plus schema version
- the wrapper can write the validated relaxation summary to JSON, matching the
  practical output path used by the M4 example
- the wrapper can emit a JSON-compatible per-step relaxation trace with
  absolute prototype time, average magnetisation, and energy terms; this is a
  small data-I/O contract, not legacy NDT, VTK/XDMF, or scheduler support
- the wrapper tracks prototype time and exposes a narrow =run_until(...)=
  method as a compatibility-shaped probe for legacy =Simulation.run_until=;
  this still uses the explicit prototype stepper and is not a production
  driver
- the wrapper can write and read a narrow JSON restart state for the
  =PrototypeSimulation= unit-square path; this captures prototype time, nodal
  magnetisation values, and parameters, not a general Finmag restart format
- =dev/dolfinx/restart_example.py= is a runnable restart-state round-trip
  witness, and the DOLFINx verification wrapper now runs it through the
  =dolfinx-restart-example= pixi task

This is intentionally not =finmag.Simulation= compatibility. It is a small API
sketch to make the next DOLFINx core path reviewable. [Codex gpt-5.5 high]

The M5 wrapper work also exposed a DOLFINx/UFL zero-form edge case. The
prototype exchange and anisotropy energy helpers now return =0.0= before form
assembly when their constants are zero, avoiding missing-domain errors for
degenerate UFL forms. Local verification: =51 passed= for =pixi run -e dolfinx
dolfinx-pytest=. [Codex gpt-5.5 high]

** Direct DOLFINx Core Simulation Port

Task 9 ports =src/finmag/sim/sim.py= directly to DOLFINx. The core
=Simulation= public surface -- construction on a DOLFINx mesh with scalar
=Ms=/=unit_length=/=name=/scalar =alpha=/scalar =gamma=; =set_m=, =m=,
=m_field=, =m_average=, =t=, =dmdt=; the interaction registry (=add=,
=get_interaction=, =interactions=/=get_interaction_list=,
=remove_interaction=, =has_interaction=, =total_energy=, =compute_energy=,
=effective_field=); integrator creation (lazy, on first =sim.integrator=
access), =set_tol=, =advance_time=, =run_until=, =reset_time=,
=reinit_integrator=; and the =sim_with= convenience factory for Exchange,
Zeeman and uniaxial anisotropy -- is preserved and driven entirely through the
already-ported =LLG=/=EffectiveField=/=Field=/=ScipyIntegrator= stack. No
compiled =finmag.native= extension, legacy =dolfin=, scheduler, table writer,
or per-simulation log file is imported; the module-scope imports are
=finmag.field=, =finmag.physics.llg=, =finmag.drivers.llg_integrator=, and the
ported =finmag.energies= Exchange/Zeeman/UniaxialAnisotropy.

The lazy export seam is updated: =finmag.__init__._LAZY_EXPORTS= now marks
=Simulation= and =sim_with= as =requires_legacy_dolfin=False=, so
=from finmag import Simulation, sim_with= resolves cleanly in the DOLFINx
environment without the legacy dolfin bridge. =finmag/sim/__init__.py='s
=from .init import *= (which imports legacy =dolfin=/=ffc= for console/file
logging setup) is wrapped in =try/except ImportError= so the package -- and
therefore =finmag.sim.sim= -- imports in the DOLFINx environment; when legacy
=dolfin= is present (the FEniCS environment) the bootstrap runs unchanged.

Deliberate deviations from the legacy module (all preserve the public names):

- =integrator_backend= defaults to ="scipy"= (was ="sundials"=) on both
  =Simulation= and =sim_with=, because the native Sundials/CVODE extension is
  unported; an explicit ="sundials"= request still raises =ImportError= by
  name via =llg_integrator=. This matches the Task 8 factory default change.
- =m= and =dmdt= return the component-blocked coordinate-ordered =xxx= arrays
  (matching the =LLG= state-vector contract and =sim.set_m='s numpy-array
  input), not raw backend dofs. On the legacy reorder-disabled DOLFIN meshes
  the two coincided; on DOLFINx's blocked layout they differ, so =xxx= is the
  faithful port. =dmdt= is read from =llg._dmdt= re-exported as =xxx= so its
  reshape((3, -1)) (and =dmdt_max=) stays component-consistent with =m=.
- =t= reports =0.0= until an integrator exists rather than the legacy
  =hasattr(self, "integrator")= path, which -- because =integrator= is a
  property that lazily *creates* the integrator -- silently built an
  integrator just to read the clock. The port guards on =has_integrator()=.
- =set_tol= updates the live integrator's =reltol=/=abstol= and calls
  =reinit()= so the new tolerances take effect; the SciPy driver has no
  in-place =set_scalar_tolerances= counterpart to the native one.
- =reset_time(t0)= rebuilds the integrator and reseeds =cur_t= and the VODE
  initial value at =t0= directly, because =ScipyIntegrator= takes no =t0=
  constructor kwarg.
- =reinit_integrator()= calls =integrator.reinit()= with no arguments; the
  legacy =debug= flag (also accepted and ignored by =set_m=) has no SciPy
  counterpart.
- =Ms= getter returns =llg.Ms= (the DG0 Field the EffectiveField and
  interactions already reference in place), and the setter delegates to
  =llg.Ms=, so an Ms update propagates to registered interactions without a
  duplicate Field.
- =Volume= is computed with a DOLFINx =assemble_scalar(Constant*dx)= MPI
  all-reduce instead of =finmag.util.meshes.mesh_volume=.
- No table writer / scheduler / per-simulation log file. =add= and
  =remove_interaction= are pure registry pass-throughs (no NDT-entity
  bookkeeping). =run_until= advances physical time straight through the
  integrator with no scheduler (scheduler integration is Task 12 scope).

Judgment-call deferrals (legacy public names retained but raising an explicit
by-name =NotImplementedError= when requested; none break import or the core
paths):

- construction options: =pbc='1d'/'2d'= (periodic boundaries), =parallel=True=
  (multi-rank), =kernel='sllg'= and =kernel='llg_stt'=;
- =sim_with= demag (=demag_solver=, default ="FK"= kept for signature fidelity
  but now raising -- pass =demag_solver=None= for a demag-free simulation) and
  DMI (=D=);
- STT: =set_stt=, =toggle_stt=, =set_zhangli=;
- scheduler: =schedule=, =unschedule=, =clear_schedule=;
- restart: =save_restart_data=, =restart= (Task 12 scope);
- output: =save_ndt=/=save_averages=, =save_m=, =save_field=, =save_vtk=,
  =save_field_to_vtk=, =snapshot=, =render_scene=, =plot_dynamics=(=_3d=),
  =plot_mesh=;
- region/material machinery: =mark_regions=, =save_m_in_region=,
  =get_submesh=, region-restricted =get_field_as_dolfin_function=;
- point probing: =probe_field=, =probe_field_along_line=;
- drivers/analyses: =relax=, =hysteresis=, =hysteresis_loop=,
  =run_normal_modes_computation=;
- =pins= callable masks (coordinate->dof mapping) -- the plain owned-index
  array form still delegates to =LLG.pins=.

Legacy public names removed outright, with no stub and no by-name
=NotImplementedError= (they now raise a plain =AttributeError=, as accepted
by review): the =magnetisation_patterns=-backed initialisers
=initialise_helix_2D=, =initialise_skyrmions=,
=initialise_skyrmion_hexlattice_2D=, =initialise_vortex=; the
=sim_helpers=-backed =skyrmion_number=/=skyrmion_number_density_function=;
the =sim_details=-backed =length_scales=/=mesh_info=; =profile= (cProfile
wrapper); =close_logfile= (per-simulation log handler teardown); and the
=instances_*= class-level instance-management family (=instances_list_all=,
=instances_delete_all=, =instances_delete_all_others=,
=instances_alive_count=) plus =shutdown=. [Claude Sonnet 5]

Dead legacy branches inside the touched core methods were removed rather than
left half-referencing deleted helpers (the parallel/llg_stt/sllg integrator
branches, the =import ipdb= double-create branch, the tablewriter-entity
registration in =add=/=remove_interaction=/=save_m_in_region=, the instance
=shutdown=/cyclic-reference machinery, and the =dt=/=T= sllg-only properties).
Lightweight instance accounting (=instance_id=, =instances=) is retained with
no cyclic references. Long-tail modules outside the core import graph
(=sim_helpers=, =sim_savers=, =hysteresis=, =magnetisation_patterns=, the
legacy scheduler) are left untouched; the core =sim.py= no longer imports
them.

New focused tests (=src/finmag/tests/test_simulation_dolfinx.py=, 28 cases)
cover the import boundary; construction and core state (mesh, scalar Ms
average, unit_length, name, default alpha/gamma, Volume); scalar alpha/gamma
round-trip and the spatially-varying-alpha rejection; =set_m= (constant and
callable) with =m=/=m_field=/=m_average= accessors and node-wise unit norm;
=t=-before-integration (asserting no silent integrator creation); =dmdt=
shape; the interaction registry pass-through and energy accessors; lazy
integrator creation, =set_tol=, =advance_time=, =reset_time=, and
=reinit_integrator=-without-integrator; the end-to-end =run_until(1e-11)=
relaxation with Exchange + Zeeman + UniaxialAnisotropy on a small 3D box
(asserting node-wise unit norm to 1e-5, a directional tilt of =<m>= toward the
+z field/easy axis, and a physical average magnitude); =sim_with= building the
three ported interactions; and every deferral above (pbc, parallel,
sllg/llg_stt kernels, scheduler, STT, restart, output, regions, hysteresis,
sim_with demag/DMI).

The import-boundary suite is extended: =finmag.Simulation= no longer reports a
missing =dolfin= in the DOLFINx environment (that assertion moved to the still
unported =finmag.NormalModeSimulation=), and a new
=test_ported_public_names_bypass_legacy_dolfin= asserts =Simulation= and
=sim_with= import with no =dolfin=/=finmag.native= in =sys.modules=.

Verified on 2026-07-21 (=pixi run -e dolfinx=):

- =dolfinx-src-simulation-pytest= reported =28 passed=;
- regressions unchanged/green: =dolfinx-src-import-pytest= =7 passed, 2
  skipped= (was 6/2 before the added ported-export test),
  =dolfinx-src-field-pytest= =26 passed=, =dolfinx-src-energies-pytest= =26
  passed=, =dolfinx-src-effectivefield-pytest= =15 passed=,
  =dolfinx-src-llg-pytest= =19 passed=, =dolfinx-src-scipy-pytest= =19
  passed=, =dolfinx-prototype-pytest= =105 passed=;
- =git diff --check= clean; only intended files changed.

The new =dolfinx-src-simulation-pytest= pixi task follows the established
focused-source-gate convention. No MPI gate is added; the serial-only guard is
inherited from =LLG.solve=/=solve_for= (and =Simulation(parallel=True)= raises
by name). Demag, DMI, PBC, stochastic kernels, STT, scheduler, restart,
NDT/VTK output, regions, hysteresis, normal modes, and the native Sundials
driver remain unported. [Claude Opus 4.8]

** First Direct-Source DOLFINx Gate (Task 10, "M5")

Task 10 is an aggregation/verification slice over Tasks 3-9, not a new porting
slice. It adds no new production =src/finmag= behaviour -- only a new
verifier script, two new pixi tasks/test files, and this documentation.

*** Naming decision

The notes above already call the Task 9 core-=Simulation= slice "M5" (see
"First M5 / DOLFINx Core Simulation Slice" and "Direct DOLFINx Core
Simulation Port"). The new aggregate verifier is therefore named
=dev/bin/verify-dolfinx-m5=, following the exact =dev/bin/verify-dolfinx-m4=
convention (frozen prototype gate) rather than inventing a
=verify-dolfinx-src-core= name. The CI workflow mirrors this as
=.github/workflows/dolfinx-m5.yml=, added because =dolfinx-m4.yml= already
proves the isolated DOLFINx pixi environment runs in this repository's CI (no
new CI infrastructure was introduced; the workflow only calls the new
verifier, exactly like the =python3-m2=/=python3-m3=/=dolfinx-m4= workflows
call their verifiers).

*** What the gate aggregates

=dev/bin/verify-dolfinx-m5= reuses
=dev/bin/validation-common.sh='s tracked-diff cleanliness guard (the same
mechanism as every other verifier here) and then runs, in order:
=dolfinx-versions=; =dolfinx-src-import=; every existing focused
=dolfinx-src-*-pytest= gate (=import-boundary=, =field=, =energies=,
=effectivefield=, =llg=, =scipy=, =simulation=); every existing MPI probe
(=dolfinx-src-field-mpi=, =dolfinx-src-energies-mpi=,
=dolfinx-src-effectivefield-mpi=); and two new Task 10 witnesses:
=dolfinx-src-deferred-pytest= and =dolfinx-src-core-smoke=. Because the script
uses =set -euo pipefail=, any single sub-gate failure stops the run and
propagates a nonzero exit; this was verified directly (see "Fail-path
evidence" below) rather than only asserted.

*** New witness 1: the core smoke (=dolfinx-src-core-smoke=)

=src/finmag/tests/simulation_dolfinx_smoke.py= is a new executable-module
witness (runnable as =python -m finmag.tests.simulation_dolfinx_smoke=,
matching the existing =*_mpi_probe.py= convention rather than a pytest file).
It builds the Task 9 end-to-end workflow -- a small 3D tetrahedral box,
=Ms=8.6e5=, =unit_length=1e-9=, =alpha=0.5=, =m_init=(1,0,0)=, Exchange
(=A=13e-12=), Zeeman (=H_ext=(0,0,1e6)=), and =UniaxialAnisotropy=
(=K1=1e5=, axis =(0,0,1)=) on the default SciPy backend -- and calls
=sim.run_until(1e-12)=. The SciPy/VODE driver steps adaptively; =run_until=
does not take a fixed =dt=. It prints a short JSON witness and asserts
=sim.t >= 1e-12= and a nodal unit-norm tolerance before printing, so a failing
physics/tolerance check exits nonzero (a plain Python =assert=, no
try/except). Measured witness on 2026-07-21:

#+begin_example
{"integrator_backend": "scipy", "m_average": [0.9802594013891642,
0.1766200460318399, 0.08889758054126375], "max_unit_norm_deviation":
2.957244739087983e-06, "t": 1e-12, "t_target": 1e-12}
#+end_example

The unit-norm tolerance is =1e-5= (matching the tolerance already accepted in
=test_simulation_dolfinx.py::test_run_until_relaxes_towards_field= for the
same integrator/relaxation combination); the measured deviation at this very
short physical time is =~3e-6=, comfortably inside it.

Observation, not a defect: running this module directly (rather than through
pytest) makes visible a pre-existing, harmless stdout/stderr side effect --
=finmag.drivers.llg_integrator= eagerly *attempts* (at import time, not at
=backend="sundials"= call time) to import
=finmag.drivers.sundials_integrator=, which triggers the legacy native-module
Makefile probe; it fails (no CVODE headers in this environment) and the
build's compiler output is printed by =finmag.util.native_compiler.run_make=
before the failure is caught and stored as
=SUNDIALS_INTEGRATOR_IMPORT_ERROR= (Task 8's by-name =ImportError= mechanism).
Every =dolfinx-src-*-pytest= gate above triggers the identical probe on
collection; pytest's default output capture just hides it there. Nothing
observes this noise as a functional problem (exit code stayed =0= throughout),
so it is not treated as a defect to fix in this aggregation-only slice.

*** New witness 2: the deferred-surfaces sweep (=dolfinx-src-deferred-pytest=)

=src/finmag/tests/test_deferred_surfaces_dolfinx.py= is the single place a
reviewer can check "unsupported features fail by name, not through incidental
import errors" (Task 10 checklist item 5) without hunting across five
per-slice suites. Investigating the brief's headline list first showed every
item already has a dedicated by-name assertion somewhere:

- demag request via =sim_with(demag_solver=...)= default -- already pinned in
  =test_simulation_dolfinx.py::test_sim_with_default_demag_is_deferred_by_name=
- DMI via =sim_with(D=...)= -- already pinned in
  =::test_sim_with_dmi_is_deferred_by_name=
- scheduler, restart, STT, =kernel="sllg"=/="llg_stt"=, =parallel=True= --
  already pinned in =test_simulation_dolfinx.py=
  (=test_scheduler_api_is_deferred=, =test_restart_is_deferred=,
  =test_stt_is_deferred=, =test_nonstandard_kernels_are_deferred=,
  =test_parallel_flag_is_deferred=)
- native Sundials/STT/thermal/multi-rank =LLG= state paths -- already pinned
  in =test_llg_dolfinx.py=
- =TimeZeeman= instantiation -- already pinned in =test_energies_dolfinx.py=
- =Field.from_expression= -- already pinned in
  =test_field_dolfinx.py::test_legacy_only_features_fail_precisely=
- =backend="sundials"= at the bare =llg_integrator(...)= factory -- already
  pinned in
  =test_scipy_driver_dolfinx.py::test_llg_integrator_sundials_backend_raises_by_name=

The new sweep file therefore does not duplicate any of those; it documents
each with a direct cross-reference and adds only genuinely new coverage: the
=integrator_backend="sundials"= path reached through =Simulation=
construction/=create_integrator=/=sim_with= (only the bare factory call was
pinned before), each asserting the same by-name =ImportError= the factory
already raises.

Investigating this sweep also surfaced one pre-existing, out-of-scope gap,
which the sweep documents rather than silently omitting or fixing: accessing
a still-fully-unported optional energy class directly (=finmag.energies.Demag=,
=DMI=, =CubicAnisotropy=, =ThinFilmDemag=, =FixedEnergyDW=, =Demag2D=,
=MacroGeometry= -- every =requires_legacy_dolfin=True= lazy export) surfaces
the raw =ModuleNotFoundError: No module named 'dolfin'= rather than a curated
message, because these modules still import legacy =dolfin= at module scope
and are not ported at all yet (Task 11+/"Later Value-Driven Slices" scope).
This is distinct from -- and does not contradict -- the demag/DMI *request*
paths above: =Simulation=/=sim_with= intercept those requests and raise a
curated =NotImplementedError= before ever reaching these modules. The gap is
a pre-existing Task 3 design point ("load only when requested"), not a
regression introduced by this gate; =test_deferred_surfaces_dolfinx.py::test_direct_legacy_only_energy_import_is_a_known_gap_not_this_slices_scope=
pins the current behaviour explicitly instead of leaving it undocumented, and
=dev/dolfinx/porting_map.md='s Near-Term Gaps section cross-references it.
No production code changes accompany this slice; per the Task 10 brief's
global constraints, a real defect found while wiring the gate is reported
rather than patched here -- this is exactly that case, and it is judged minor
and pre-existing rather than blocking.

*** Fail-path evidence

The Task 10 verification step requires evidence that the aggregate fails when
a sub-gate fails, not just an assertion that it would. This was demonstrated
directly rather than only argued from =set -e= semantics: the core smoke's
unit-norm tolerance was temporarily edited to an impossible value
(=UNIT_NORM_TOL = 1e-30=) in the working tree, =dev/bin/verify-dolfinx-m5= was
re-run and exited =1= with the =AssertionError= surfaced through the failing
=pixi run -e dolfinx dolfinx-src-core-smoke= step, and the file was then
restored to its original =1e-5= tolerance (confirmed identical to the
pre-edit copy by diff) before anything was committed. Every other sub-gate's
failure path is the same =pixi run= non-zero-exit-under-=set -e= propagation
already relied on by =verify-dolfinx-m4= and the =verify-python3-*= family; it
was not separately broken and restored for each one.

*** Supported API surface (this gate)

- =finmag= top-level lazy exports: =Simulation=, =sim_with=, =Field=, version
  access, plus =finmag.energies.{Exchange, Zeeman, UniaxialAnisotropy,
  TimeZeeman-and-siblings-as-deferred-stubs}=.
- =Field=: constants, callables, =set=, =from_array=, =from_field=,
  =from_function=, scalar/vector inspection, raw arrays, coordinate-ordered
  =xyz=/=xxx=, volume averages, normalisation, VTK/XDMF output (write-only).
  Serial and two-rank owned/ghost contracts are tested.
- =Exchange=/=Zeeman=/=UniaxialAnisotropy=: scalar =A=/=K1=/=K2=/constant
  axis/constant field only; box-assemble is the only supported energy method.
- =EffectiveField=: full registry (=add=/=get=/=exists=/=all=/=remove=, total
  field/energy, time-update callbacks).
- =LLG=: scalar =Ms=/=alpha=, =gamma=, =set_m=, =solve=/=solve_for=, serial
  only, pinning retained.
- Driver: SciPy is the default and only supported DOLFINx backend
  (=integrator_backend="scipy"=); real reinitialisation from current state.
- =Simulation=/=sim_with=: construction, scalar =Ms=/=unit_length=/=name=/
  =alpha=/=gamma=, =set_m=/=m=/=m_field=/=m_average=/=t=/=dmdt=, the
  interaction registry and energy accessors, integrator
  creation/tolerances/=advance_time=/=run_until=/=reset_time=/
  =reinit_integrator=, and =sim_with= for Exchange/Zeeman/UniaxialAnisotropy.

*** Deferred surfaces (this gate)

Pointer to the authoritative lists: =dev/dolfinx/porting_map.md='s "Near-Term
Gaps" section and the "Judgment-call deferrals"/"Legacy public names removed
outright" lists in the "Direct DOLFINx Core Simulation Port" section above.
Headline items, all raising a curated =NotImplementedError=/=ImportError= by
name (see the sweep above for exact cross-references): demag, DMI, PBC,
=kernel="sllg"=/="llg_stt"=, =parallel=True=, scheduler, restart, NDT/VTK/plot
output, regions, hysteresis, normal modes, STT/Zhang-Li, and
=integrator_backend="sundials"=. Known non-goal documented but not fixed here:
directly constructing a still-unported optional energy class
(=Demag=/=DMI=/=CubicAnisotropy=/etc.) surfaces the raw legacy-=dolfin=
=ModuleNotFoundError= rather than a curated message.

*** Measured results (2026-07-21, =pixi run -e dolfinx=, oracle-equivalent to a
clean =dev/bin/verify-dolfinx-m5= run)

- =dolfinx-versions=: dolfinx =0.10.0=, python =3.12.13=, numpy =2.4.6=, scipy
  =1.18.0=, petsc4py =3.25.2=, Open MPI =5.0.10=, mpi4py =4.1.2=.
- =dolfinx-src-import-pytest= =8 passed, 2 skipped=; =dolfinx-src-field-pytest=
  =26 passed=; =dolfinx-src-field-mpi= ok (2 ranks, =16= global vertices);
  =dolfinx-src-energies-pytest= =26 passed=; =dolfinx-src-energies-mpi= ok;
  =dolfinx-src-effectivefield-pytest= =15 passed=;
  =dolfinx-src-effectivefield-mpi= ok; =dolfinx-src-llg-pytest= =19 passed=;
  =dolfinx-src-scipy-pytest= =19 passed=; =dolfinx-src-simulation-pytest=
  =30 passed=; =dolfinx-src-deferred-pytest= =5 passed= (new);
  =dolfinx-src-core-smoke= witness printed above, exit =0= (new).
- =git diff --check= clean; tracked-diff guard reported no tracked-file
  changes across the full run; only the intended new/edited files changed.

[Claude Sonnet 5]

** Native array-only FK BEM for DOLFINx (Task 11a)

Task 11a is the first half of "Port FK demag" (plan items 1-2 only): separate
the array-only LLG/BEM native bindings from the legacy SWIG-DOLFIN mesh
converters and the =-ldolfin= link dependency, and rebuild/test that surface on
Python 3.12 without installing legacy DOLFIN. The remaining items (DOLFINx
boundary-mesh probes, porting =src/finmag/energies/demag=, oracle BEM
comparisons, the barmini FK-demag workflow) are Task 11b and are *not* started
here.

*** The binding-separation problem

The compiled FK BEM lived in =native/src/llg/bem.cc=, registered by the =llg=
module (=native/src/llg/py_llg_module.cc=), which links =-ldolfin= and calls
=finmag::util::register_dolfin_swig_converters()=. =bem.cc= contained both the
legacy DOLFIN =BoundaryMesh= entry points (=compute_bem<>= using
=df::BoundaryMesh=, =CellIterator=, etc.) and the array-based
=compute_bem_from_arrays<>= / =compute_lindholm_formula<>= kernels added during
the M3/FEniCS-2019 work. The array kernels touch only =np_array= and =vector3=,
but they shared a translation unit and an umbrella header (=finmag_includes.h=,
which unconditionally =#include <dolfin.h>= and =#include <cvode/cvode.h>=) with
the DOLFIN-coupled code, so nothing array-only could compile in the =dolfinx=
env (no libdolfin, no SUNDIALS, Python 3.12).

*** Module/target design

Following the established pixi Boost.Python pattern (how =sundials.so= is handled
as a conditional pixi module in =native/Makefile=), a *new* module target was
added rather than mutating the legacy =llg.so= target, so the legacy M3 lane
keeps building unchanged:

- =native/src/llg/bem_arrays.h= (new): the array-only Lindholm/BEM template
  kernels (=lindholm_formula<>=, =compute_bem_from_arrays<>=,
  =compute_lindholm_formula<>=), moved verbatim out of =bem.cc= into a shared,
  dolfin-free header. Single source of truth; no numerical change.
- =native/src/llg/bem_arrays.cc= (new): =BOOST_PYTHON_MODULE(bem_arrays)=
  registering =compute_bem_fk_from_arrays=, =compute_bem_gcr_from_arrays=,
  =compute_lindholm_L=, =compute_lindholm_K=. No dolfin/SWIG-DOLFIN symbols.
- =native/src/llg/bem.cc= (edited): now =#include "bem_arrays.h"= and keeps only
  the DOLFIN =BoundaryMesh= path and the combined =register_bem()=; the legacy
  =llg= surface is byte-for-byte behaviourally unchanged (verified: it still
  reproduces the same golden Lindholm/BEM values after the refactor).
- =native/src/finmag_includes.h= (edited): =#include <dolfin.h>= and
  =#include <cvode/cvode.h>= are guarded by =FINMAG_NO_DOLFIN= /
  =FINMAG_NO_SUNDIALS=; when dolfin.h is skipped, the standard headers that used
  to arrive transitively through it (=<iostream>=, =<vector>=, =<string>=,
  =<stdexcept>=, =<csignal>=, ...) are pulled in explicitly.
- =native/src/llg/vector3.h= (edited): the two =dolfin::Vertex= / =dolfin::Point=
  constructors (used only by the legacy BoundaryMesh path) are guarded by
  =FINMAG_NO_DOLFIN=.
- =native/Makefile= (edited): a new =IS_DOLFINX_ENV= branch (matched on
  =/envs/dolfinx= in the Python prefix, checked *before* the general =/.pixi/=
  branch) sets =MODULES = bem_arrays.so=, restricts =SOURCES= to
  =bem_arrays.cc= + =np_array.cc=, and adds
  =-DFINMAG_NO_DOLFIN -DFINMAG_NO_SUNDIALS= to =CFLAGS_NOPCH= (so it also applies
  to the precompiled-header build). The =bem_arrays.so= target links only
  =bem_arrays.o np_array.o= with empty LDFLAGS -- no =-ldolfin=, no
  =swig_dolfin.o=. The legacy =llg.so=/=sundials.so=/=llb.so= branch is
  untouched.

*** Toolchain / pixi.lock

The base =dolfinx= feature shipped =gcc= and boost headers but no =g++=, no
=make=, and no =libboost_python=. Added to =[feature.dolfinx.dependencies]=:
=cxx-compiler=, =make=, =libboost-python-devel= (provides =libboost_python312.so=
matching boost 1.88). This regenerated =pixi.lock= (dolfinx env only; the
default FEniCS-2019 env is unchanged). This is a native-build toolchain
addition kept off the runtime import path.

Also added =[feature.dolfinx.activation.env] WRITE_FINMAG_VERSION = "0"=: the
DOLFINx lane can now compile a native surface, and importing finmag can trigger
that =make= as a side effect, so this keeps implicit builds from rewriting the
tracked =src/finmag/__version__.py= (the read-only validators already use this
convention).

*** Import-boundary fix (no native leak on the plain import path)

Making the native build *succeed* in the DOLFINx env exposed a latent coupling:
=finmag/drivers/llg_integrator.py= eagerly did
=from finmag.drivers.sundials_integrator import SundialsIntegrator= at module
scope, which imports =finmag.native=. On HEAD this stayed clean only *by
accident* -- the native build failed in the DOLFINx env, so Python unwound the
partial =finmag.native= import. With an array-only surface now buildable, the
=finmag.native= package =__init__= succeeds (building =bem_arrays.so=) before the
still-unported =sundials= submodule fails, leaving =finmag.native= in
=sys.modules= and violating the "plain import loads no native modules" boundary
asserted by =test_import_boundary= / =test_scipy_driver_dolfinx= /
=test_deferred_surfaces_dolfinx=.

Fix (=llg_integrator.py=): the sundials availability probe is now lazy. A PEP 562
module =__getattr__= resolves =SundialsIntegrator= /
=SUNDIALS_INTEGRATOR_IMPORT_ERROR= on first *access* (so the test sentinels and
the =backend="sundials"= raise-by-name path are unchanged and still reflect real
availability), and the plain =import finmag= / =sim_with= path never touches the
attribute, hence never imports =finmag.native=. When the probe fails, any
=finmag.native*= modules it added to =sys.modules= are rolled back, reproducing
HEAD's "graceful sundials absence with no native residue" without depending on a
build failure. On the legacy stack the probe succeeds and nothing is removed.

*** Test / verification evidence (2026-07-21, =pixi run -e dolfinx=)

New focused test =src/finmag/tests/test_native_bem_arrays_dolfinx.py= + pixi task
=dolfinx-src-native-bem-pytest= (=WRITE_FINMAG_VERSION=0=). The golden values are
captured from the known-good compiled =finmag.native.llg= in the FEniCS-2019
lane (the =dev/bin/native-fk-bem-smoke.py= reference path). Coverage:

- module imports in the dolfinx env with =dolfin not in sys.modules= and no
  =finmag.native.llg=;
- =compute_bem_fk_from_arrays= on the =BoundaryMesh(UnitCubeMesh(1,1,1))= arrays
  (8 vertices, 12 triangles) reproduces the legacy compiled BEM matrix to
  =atol=1e-14= (exact equivalence), plus finite/shape and =b2g= passthrough;
- solid-angle identity: FK BEM row sums equal =-1= (=B @ 1 = -1=, the
  Fredkin-Koehler double-layer-of-a-constant identity) to =atol=1e-12=;
- diagonal entries are negative (=solid_angle/4pi - 1 < 0=; uniform =-0.875= for
  the congruent cube corners);
- =compute_bem_gcr_from_arrays= finite; =compute_lindholm_L= matches the legacy
  golden and is invariant under a rigid rotation applied to all four points;
  =compute_lindholm_K= finite.

Result: =dolfinx-src-native-bem-pytest= = =8 passed=. RED evidence: before the
Makefile/source wiring, the same task failed with
=ModuleNotFoundError: No module named 'finmag.native.bem_arrays'=.

Regression gates (all green): =dolfinx-src-import-pytest= =8 passed, 2 skipped=;
full =dev/bin/verify-dolfinx-m5= exit =0= with the tracked-diff guard reporting
no tracked-file changes; legacy =dev/bin/verify-python3-native-fk-bem-smoke=
green from a clean native build (=llg.so= with full DOLFIN coupling rebuilds and
=compute_bem_fk_from_arrays= still returns the expected =(8, 8)= matrix);
=git diff --check= clean.

The Task 11 plan checkboxes are intentionally left unticked; items 1-2 are
delivered by 11a and the whole of Task 11 is ticked when 11b completes the FK
demag capability.

[Claude Opus 4.8]

** Direct DOLFINx FK Demag Port (Task 11b)

Task 11b is the second half of "Port FK demag": the direct DOLFINx port of the
Fredkin-Koehler solver in =src/finmag/energies/demag/fk_demag.py=, wired into
=Simulation.add(Demag())= / =sim_with(demag_solver="FK")=, with all of Task 11's
plan checkboxes now ticked across 11a+11b.

*** Formulation mapping (legacy step -> DOLFINx step)

The discrete two-potential formulation is preserved verbatim; only the FEM-API
layer changed.

- =S1 = df.FunctionSpace(mesh, "Lagrange", 1)= -> =associated_scalar_space(m.functionspace)=.
- Neumann stiffness =assemble(dot(grad(u),grad(v))*dx)= -> PETSc
  =assemble_matrix(poisson_form)=; a constant =PETSc.NullSpace= is attached for
  the singular pure-Neumann =phi_1= solve (the additive constant is irrelevant
  to =H=-grad(phi)=).
- Legacy =KrylovSolver= (default CG) -> =PETSc.KSP= CG with the legacy tolerance
  dict mapped to =rtol/atol/max_it= and the legacy preconditioner name mapped to
  a PETSc PC (=default->hypre=, =ilu=, =none=, ...). =solver_type='LU'= is
  deferred by name (the singular Neumann matrix has no direct factorisation
  without pinning); =macrogeometry= / =MacroGeometry= / =Demag2D= / non-FK
  solvers raise =NotImplementedError= by name.
- =_Ms_times_divergence * m.vector= (matrix-vector) -> a linear form
  =assemble_vector(Ms.f*inner(m.f, grad(v))*dx)= re-assembled each solve (m
  changes); likewise the gradient =_gradient * phi.vector= -> linear form
  =assemble_vector(inner(v3, -grad(phi))*dx)= divided by unscaled nodal volumes.
  This avoids assembling rectangular S3<->S1 operators and matches the box
  pattern already used by =energy_base.py=.
- =phi_2= Dirichlet data: =DirichletBC(S1, phi_2, DomainBoundary())= ->
  =fem.dirichletbc(g_bc, boundary_dofs)= where =g_bc= is a Function whose
  boundary dofs are set to =B @ phi_1|_bnd= each solve; the stiffness matrix is
  assembled once with the BC, and each solve applies =apply_lifting= + =set_bc=
  to the (zero) RHS.
- Energy =-0.5 mu0 int H.M dx * unit_length**dim= and the lumped nodal energy
  density are assembled exactly as before (the density's =unit_length**dim=
  numerator/denominator cancel, so it is assembled in mesh units).

*** Boundary-ordering contract and its pinning evidence

The one genuinely new mechanic is =boundary_bem_arrays(domain, S1)=, which feeds
the compiled =compute_bem_fk_from_arrays= (Task 11a). The contract:

- BEM-local node =i= is a *CG1 boundary degree of freedom* of S1
  (=locate_dofs_topological= on the exterior facets); =coords[i]= is that dof's
  coordinate and =b2g[i]= is its S1 dof index, so =phi.x.array[b2g]= gathers
  boundary values in BEM-local order and the reverse scatter writes them back
  (no vertex/dof confusion -- the whole pipeline stays in S1 dof space).
- Boundary triangles are taken from the exterior facets and *explicitly
  oriented outward* (flip the winding when =cross(p1-p0,p2-p0).(p0-p_opposite) <
  0=); the sorted-index winding DOLFINx returns from facet->vertex connectivity
  is *not* consistent and gives a wrong (though row-sum -1) BEM.
- BEM-local indices for each triangle vertex are resolved by *coordinate*, so
  the mapping is robust to any DOLFINx submesh/entity reordering.

Pinned bit-for-bit: on the exact legacy Kuhn (main-diagonal) split of the unit
cube -- and, separately, on =create_unit_cube(1,1,1)=, which DOLFINx builds with
the same split -- the extracted BEM equals the Task 11a golden matrix to
=atol=1e-13= after the coordinate-induced row/col permutation
(=test_extraction_reproduces_golden_bem_*=). Without the outward-orientation
step the max entry error is ~0.18; with it, ~1e-17.

*** Fixture provenance

=src/finmag/tests/fixtures/fk_demag_oracle.json= holds coordinate-ordered
(lexicographic xyz) legacy references generated at the frozen oracle commit
=ba9280934e188d7f3800e7b9865e70a9422f7687= via =dev/bin/run-legacy-oracle= (the
unmodified pre-port =fk_demag.py=, which is byte-identical to the oracle commit).
Two cases: a uniformly magnetised =UnitCubeMesh(4,4,4)= (m=(1,0,0), Ms=8e5) and
the barmini bar =BoxMesh(Point(0,0,0),Point(3,3,10),2,2,4)= (m=(1,0,1),
Ms=0.86e6), unit_length=1e-9. The BEM golden matrix itself is reused from the
11a fixture. The cube also carries an *analytic* anchor stronger than any
numeric oracle: average demag field =-Ms/3= and energy =mu0 Ms^2 V / 6= (cube
demag factor 1/3 per axis).

*** Measured results (2026-07-21, =pixi run -e dolfinx=, serial)

DOLFINx =create_box=/=create_unit_cube= reproduce the legacy =BoxMesh=/
=UnitCubeMesh= vertex-for-vertex, so the comparison is coordinate-identical:

- cube (4x4x4): energy rel diff =4.7e-8=; average-field rel diff ~=5e-6=;
  pointwise field max abs diff / |H|_max = =6.9e-6=.
- barmini (2x2x4): energy rel diff =2.3e-9=; average-field rel diff ~=1e-7=;
  pointwise = =4.7e-6=.

The residual ~5e-6 was *suspected* to be the Krylov tolerance (=1e-6=), not the
method -- but simply tightening the port's own KSP rtol to =1e-12= alone left
the pointwise gap unchanged at =6.4e-6= (review round 1, Finding 1), which
falsified that claim as originally stated. The actual cause was demonstrated
empirically instead of asserted: the *oracle fixture itself* is frozen at the
legacy default =1e-6= Krylov tolerance. Regenerating the cube datum at the
oracle with both legacy Krylov solves (=phi_1=, =phi_2=) tightened to =1e-12=
and comparing it against the port at the same tightened KSP tolerance collapses
the pointwise gap to ~=1.4e-12= and the energy gap to ~=5.5e-14= -- see
=cube_tight_tolerance= in the regenerated
=src/finmag/tests/fixtures/fk_demag_oracle.json= (schema v1,
=gen_fk_demag_oracle.py=) and
=test_oracle_cube_tight_tolerance_isolates_krylov_residual=. This proves the
standard-tolerance residual is the oracle's own frozen 1e-6 solves, not a
systematic difference between the port and the legacy formulation. Declared
gate tolerances: energy rel =1e-5=, average rel =1e-3=, pointwise/|H|max =1e-4=,
BEM golden =1e-13=. The barmini =sim_with(..., demag_solver="FK")= workflow
builds, computes demag energy =9.99e-21 J=, and integrates
(=run_until(5e-12)=) with compiled FK demag present. The uniform-cube analytic
checks hold on a 6x6x6 mesh (avg Hx within 10% of =-Ms/3=, energy within 10% of
=mu0 Ms^2 V/6=; the coarse-mesh FK field converges toward the continuum result
from above).

*** Makefile build-hygiene (both lanes)

The three mandatory 11a-review carry-ins are done and verified on *both* lanes:

1. Per-env object tree =native/obj/$(PYTHON_PREFIX_TAG)/= and per-env
   =PRECOMPILED_HEADER_DIR=, keyed on the active Python prefix, so the legacy
   (libdolfin, NumPy-1 ABI) and DOLFINx (NumPy-2, =-DFINMAG_NO_DOLFIN=) builds
   compile shared units such as =np_array.cc= into disjoint paths and can never
   silently cross-link. Verified: the DOLFINx lane rebuilds =bem_arrays.so= and
   passes =dolfinx-src-native-bem-pytest=; the legacy lane rebuilds the full
   =-ldolfin= native surface and =dev/bin/verify-python3-native-fk-bem-smoke=
   returns the expected =(8,8)= matrix -- objects land in
   =obj/..._envs_default/= vs =obj/..._envs_dolfinx/= respectively. (This is
   also the exact hazard that made a co-collected pytest run fail: loading the
   stale NumPy-1 =sundials.so= into a NumPy-2 process corrupts the array ABI so
   the NumPy-2 =bem_arrays= rejects arrays; the per-env split and separate gate
   processes both avoid it.)
2. =bem_arrays.cc= is excluded from the legacy branches' =SOURCES= (it is
   compiled but never linked there).
3. =dolfinx-src-native-bem-pytest= and the new =dolfinx-src-demag-pytest= are
   folded into =dev/bin/verify-dolfinx-m5=.

*** Deferred / deliberate deviations

=solver_type='LU'=, =macrogeometry=/=MacroGeometry=, =Demag2D=, and the
=Treecode=/=GCR= solvers all raise =NotImplementedError= by name. The Task 10
fail-forward pins that expected =ModuleNotFoundError= for =Demag()= /
=sim_with(demag_solver="FK")= are replaced with curated-behaviour assertions
(=Demag()= now builds an =FKDemag=; the still-unported =DMI=/=CubicAnisotropy=/
=ThinFilmDemag=/=FixedEnergyDW= remain the documented Task 3 gap). The
=test_ported_simulation_does_not_load_legacy_dolfin_or_native= import-boundary
check now runs in a fresh subprocess so it is order-independent once the opt-in
demag interaction has loaded =finmag.native= elsewhere in the suite.

[Claude Opus 4.8]
