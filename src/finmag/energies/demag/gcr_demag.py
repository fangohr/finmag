class GCRDemag(object):

    """
    Historical placeholder for the old GCR demag solver.

    The Python 3 transition intentionally does not port this solver. The name
    is kept only so the codebase still records that such a solver existed. [Codex GPT-5.4]
    """

    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "Historical GCR demag solver is not ported to the Python 3 path.")
